(async function(){
 'use strict';
 const C=window.MixCore,$=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const paths={glass:'<path d="M4 4h16l-8 9L4 4Zm8 9v7m-5 0h10"/>',chevron:'<path d="m9 6 6 6-6 6"/>',book:'<path d="M4 4h6a3 3 0 0 1 2 1 3 3 0 0 1 2-1h6v15h-6a3 3 0 0 0-2 1 3 3 0 0 0-2-1H4V4Zm8 1v15"/>',bottle:'<path d="M9 3h6m-5 0v5l-3 4v8h10v-8l-3-4V3M7 14h10"/>',whiskey:'<path d="M9 3h6v3l2 2v3l2 2v8H5v-8l2-2V8l2-2V3Zm-2 9h10v6H7v-6Z"/>',gin:'<path d="M9 3h6m-5 0v5c0 1-3 2-3 5v8h10v-8c0-3-3-4-3-5V3m-5 11h6m-3 4c-2-1-2-3 0-4 2 1 2 3 0 4Z"/>',rum:'<path d="M9 3h6m-5 0v4l-3 3v11h10V10l-3-3V3M7 12h10M9 15l6 4m0-4-6 4"/>',tequila:'<path d="M10 3h4m-3 0v6l-3 3v9h8v-9l-3-3V3m-3 12h4m-7 6-3-3m3 3-1-5m11 5 3-3m-3 3 1-5"/>',vodka:'<path d="M9 3h6m-5 0v4L8 9v12h8V9l-2-2V3m-6 8h8m-4 3 2 2-2 2-2-2 2-2Z"/>',brandy:'<path d="M10 3h4m-3 0v5c0 1-5 4-5 8 0 3 2 5 6 5s6-2 6-5c0-4-5-7-5-8V3m-5 12h8m-7 3h6"/>',amaro:'<path d="M9 3h6m-5 0v4c0 1-3 3-3 6v8h10v-8c0-3-3-5-3-6V3M9 14h6m-3-3c1-2 2-2 3-2"/>',bitters:'<path d="M10 3h4m-3 0v4l-3 3v11h8V10l-3-3V3M8 12h8"/><path d="M18 5c0-1 1-2 2-2 0 1 1 2 1 3a1.5 1.5 0 0 1-3 0Z"/>',beer:'<path d="M6 8v12h10V8H6Zm10 3h2a2 2 0 0 1 0 4h-2M6 8c0-2 1-3 3-3 1-2 4-2 5 0 2 0 3 1 3 3M9 11v6m4-6v6"/>',wine:'<path d="M10 2h4v6c0 1 1 2 3 3v10H7V11c2-1 3-2 3-3V2Zm-2 11h8v6H8v-6Z"/>',liqueur:'<path d="M10 3h4m-3 0v4l-4 4v8c0 2 10 2 10 0v-8l-4-4V3M7 13c3 1 7 1 10 0m-7 4h4"/>',aperitif:'<path d="M4 7h14l-7 7-7-7Zm7 7v6m-4 0h8M16 4l4 4m-4 0 4-4"/>',juice:'<path d="M7 6h10l1 15H6L7 6Zm1 4h9M9 6l2-3h5m-2 0 3 3"/>',produce:'<path d="M12 8c-2-3-7-2-8 3-1 5 3 10 8 10s9-5 8-10c-1-5-6-6-8-3Zm0 0c0-3 1-5 4-5m-4 3c-2-2-4-2-5-1"/>',syrup:'<path d="M9 4h6m-5 0v4l-3 3v10h10V11l-3-3V4M8 13h8m1-10h3v3m-3-1h3"/>',seasoning:'<path d="M8 7h8l2 14H6L8 7Zm0 4h8M9 3h.1m2.9 0h.1m2.9 0h.1"/>',soda:'<path d="M8 5h8l1 16H7L8 5Zm0 4h8M9 3h6"/><circle cx="19" cy="5" r="1"/><circle cx="20" cy="10" r="1"/>',food:'<path d="M6 8h12l-1 13H7L6 8Zm-1 0h14M8 5h8m-5-2h2M9 12h6m-3 0v5"/>',cloud:'<path d="M7 17H6a4 4 0 1 1 1-7 6 6 0 0 1 12-1 4 4 0 0 1-1 8h-1m-5 4V11m-3 3 3-3 3 3"/>',check:'<path d="m5 12 4 4L19 6"/>',plus:'<path d="M12 5v14M5 12h14"/>',search:'<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 4 4"/>',heart:'<path d="M20 5a5 5 0 0 0-8 1 5 5 0 0 0-8-1c-4 4 0 9 8 15 8-6 12-11 8-15Z"/>',close:'<path d="m6 6 12 12M6 18 18 6"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',filter:'<path d="M4 7h16M7 12h10m-7 5h4"/>',minus:'<path d="M5 12h14"/>',download:'<path d="M12 3v12m-4-4 4 4 4-4M4 17v4h16v-4"/>',upload:'<path d="M12 16V4m-4 4 4-4 4 4M4 17v4h16v-4"/>',edit:'<path d="m15 4 5 5M4 20l5-1L20 8a2 2 0 0 0-5-5L4 14v6Z"/>',copy:'<rect x="8" y="8" width="12" height="12" rx="2"/><path d="M15 8V4H4v11h4"/>',trash:'<path d="M3 6h18M9 6V3h6v3M5 6l1 15h12l1-15M10 10v7m4-7v7"/>',info:'<circle cx="12" cy="12" r="9"/><path d="M12 11v5m0-9v.1"/>',refresh:'<path d="M20 11a8 8 0 0 0-14-5L3 9m0-6v6h6M4 13a8 8 0 0 0 14 5l3-3m0 6v-6h-6"/>',offline:'<path d="M4 4 20 20M8 8a6 6 0 0 1 10 3 4 4 0 0 1 2 6M7 10a4 4 0 1 0-1 8h10"/>',bookmark:'<path d="M6 3h12v18l-6-4-6 4V3Z"/>'};
 const icon=name=>`<svg viewBox="0 0 24 24" aria-hidden="true">${paths[name]||paths.glass}</svg>`;
 const pantryIcons={whiskey:'whiskey',gin:'gin',rum:'rum',tequila:'tequila',vodka:'vodka','brandy-root':'brandy',amaro:'amaro',bitters:'bitters',beer:'beer',wine:'wine',vermouth:'wine','port-sherry':'wine','liqueur-root':'liqueur',aperitif:'aperitif',juice:'juice','fruit-vegetable':'produce',syrup:'syrup',seasoning:'seasoning','soda-root':'soda','other-food':'food'};
 let pantryMenus=window.MixHierarchy?.get?.()||[];
 const pantryMenu=id=>pantryMenus.find(([root,items])=>root===id||items.includes(id));
 const pantryCategoryIcon=id=>pantryIcons[id]||'bottle';
 const glassPaths={
  cocktail:'<path d="M3.5 4h17L12 13 3.5 4Zm8.5 9v7m-5 0h10"/>',
  coupe:'<path d="M4 6c.5 4 3.4 6 8 6s7.5-2 8-6H4Zm8 6v8m-5 0h10"/>',
  flute:'<path d="M7 3h10l-1.2 9a3.8 3.8 0 0 1-7.6 0L7 3Zm5 13v4m-4 0h8"/>',
  wine:'<path d="M6.5 3h11l-1 7a4.5 4.5 0 0 1-9 0l-1-7ZM12 15v5m-4 0h8"/>',
  highball:'<path d="M6 3h12l-1 18H7L6 3Zm1 4h10"/>',
  rocks:'<path d="M5 7h14l-1 14H6L5 7Zm1 4h12"/>',
  hurricane:'<path d="M7 3h10c0 4-2.5 5-2.5 8s2 4 1.5 6H8c-.5-2 1.5-3 1.5-6S7 7 7 3Zm5 14v3m-4 0h8"/>',
  goblet:'<path d="M5 4h14l-1 6a6 6 0 0 1-12 0L5 4Zm7 12v4m-4 0h8"/>',
  mug:'<path d="M5 5h11v16H6L5 5Zm11 3h2a3 3 0 0 1 0 6h-2"/>',
  julep:'<path d="M6 4h12l-1 17H7L6 4Zm-1 0h14M7 8h10"/>',
  tiki:'<path d="M6 3h12l-1 18H7L6 3Zm2 4 2 2m6-2-2 2m-5 5h6m-7 3 2 2m6-2-2 2"/>',
  glass:'<path d="M5 5h14l-1 16H6L5 5Zm1 5h12"/>'
 };
 const glassTypes=[
  {name:'马天尼杯',kind:'cocktail',match:/马天尼|鸡尾酒杯/},
  {name:'碟形杯',kind:'coupe',match:/碟形|高脚碗/},
  {name:'香槟笛杯',kind:'flute',match:/香槟|笛/},
  {name:'葡萄酒杯',kind:'wine',match:/葡萄酒/},
  {name:'古典杯',kind:'rocks',match:/古典|平底|大号杯/},
  {name:'高球杯',kind:'highball',match:/海波|柯林斯|高杯|细长高杯/},
  {name:'高脚杯',kind:'goblet',match:/Copo|高脚杯/},
  {name:'飓风杯',kind:'hurricane',match:/飓风/},
  {name:'马克杯',kind:'mug',match:/铜|咖啡/},
  {name:'朱利普杯',kind:'julep',match:/朱利普/},
  {name:'Tiki 杯',kind:'tiki',match:/Tiki|陶/}
 ];
 function glassType(name=''){return glassTypes.find(type=>type.match.test(name))}
 function glassKind(name=''){return glassType(name)?.kind||'glass'}
 const glassIcon=name=>`<svg class="glass-svg" viewBox="0 0 24 24" aria-hidden="true">${glassPaths[glassKind(name)]}</svg>`;
 let db,storageError='',env={data:C.validate(C.clone(window.MIX_SEED)),base:null,syncId:'',sha:null,lastSync:null},view='recipes',filters={q:'',tags:[],base:'',sourceName:'',availability:'none',favorite:false},sort='recent',pantryQ='',pantryTags=[],onlyOwned=false,filterOpen=false,pantryFilterOpen=false,syncBusy=false,saveTimer=null,toastTimer=null,editorId=null,lastFocus=null,pantrySection='all',pantryOpen=new Set(['whiskey']);
 function applyTaxonomy(data){
  const taxonomy=window.MIX_TAXONOMY;if(!taxonomy)return data;
  const canonical=window.MIX_SEED.ingredients,ids=new Set(canonical.map(i=>i.id)),custom=data.ingredients.filter(i=>i.customized&&!ids.has(i.id));
  data.ingredients=[...C.clone(canonical),...custom];
  const used={tags:data.recipes.flatMap(r=>r.tags||[]),glasses:data.recipes.map(r=>r.glass),sources:data.recipes.map(r=>r.sourceName)};
  for(const key of Object.keys(taxonomy.recipe))data.options[key]=[...new Set([...taxonomy.recipe[key],...(used[key]||[])])].filter(Boolean);
  return C.validate(data);
 }
 let pantryDraft=null,editorDraft=null,editorMode='recipe',editorVersion='',selectedVersion='',itemContext='',itemId='';
 let pantryBrowser=null,pickerBrowser=null,itemReturn=null;const dialogFocus=new WeakMap();
 const editing=()=>!!pantryDraft||!!$('#editor-dialog')?.open||!!$('#item-dialog')?.open||!!$('#material-dialog')?.open;
 let config={owner:'',repo:'',branch:'',path:'mixbook.json',auto:false},token='',remember=false;
 try{config={...config,...JSON.parse(localStorage.getItem('mixbook.config')||'{}')};token=sessionStorage.getItem('mixbook.token')||localStorage.getItem('mixbook.token')||'';remember=!!localStorage.getItem('mixbook.token')}catch{}
 try{
   db=await new Promise((resolve,reject)=>{const req=indexedDB.open('mixbook',1);req.onupgradeneeded=()=>req.result.createObjectStore('state');req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)});
   const loaded=await dbRead('current');if(loaded){if(loaded.data.schemaVersion<4)await dbWrite('backup-before-v4',loaded);env={...env,...loaded,data:applyTaxonomy(C.installCatalog(loaded.data,window.MIX_SEED))};if(env.base)env.base=applyTaxonomy(C.installCatalog(env.base,window.MIX_SEED))}await dbWrite('current',env);
 }catch{storageError='浏览器未能读取本地数据。当前为临时会话，请先导出备份；不要清理浏览器数据。';db=null}
 function dbRead(key){return new Promise((resolve,reject)=>{const tx=db.transaction('state','readonly'),req=tx.objectStore('state').get(key);req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}
 function dbWrite(key,value){return new Promise((resolve,reject)=>{const tx=db.transaction('state','readwrite');tx.objectStore('state').put(value,key);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error)})}
 let writeQueue=Promise.resolve();
 async function persist(next){if(db)await dbWrite('current',next);env=next}
 function update(change){if(syncBusy){toast('正在同步，请稍后再修改。');return Promise.resolve(false)};const task=writeQueue.then(async()=>{const next=C.clone(env);change(next.data);next.data=C.validate(next.data);await persist(next);scheduleSync();return true}).catch(e=>{toast('保存失败：'+e.message);return false});writeQueue=task;return task}
 function toast(msg){const el=$('#toast');el.textContent=msg;el.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.classList.remove('show'),4200)}
 function dirty(){return !env.base||!C.same(env.data,env.base)}
 function status(){return syncBusy?'正在同步…':storageError?'临时会话':config.repo&&env.lastSync?(dirty()?'有待同步修改':'已同步'):'已存本机'}
 function dateText(v){return v?new Date(v).toLocaleString('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'}):'尚未同步'}
 function ingredient(id){return env.data.ingredients.find(x=>x.id===id)}
 function ingredientName(id){return ingredient(id)?.name||'未知材料'}
 function recipeIngredientName(id,data=env.data){const i=data.ingredients.find(x=>x.id===id);if(!i)return'未知材料';const roots=new Set(pantryMenus.map(entry=>entry[0])),pages=new Set(pantryMenus.flatMap(entry=>entry[1]));if(roots.has(id)||pages.has(id))return i.name;const path=C.ingredientPath(id,data),page=[...path].reverse().find(x=>pages.has(x.id))||(i.tags||[]).map(tag=>data.ingredients.find(x=>x.id===tag)).find(x=>pages.has(x?.id));return page?`${page.name} · ${i.name}`:i.name}
 function pantryHas(d,id){return d.pantryItems.some(i=>i.matches.includes(id))}
 function pantryAdd(d,material){if(pantryHas(d,material.id))return;d.pantryItems.push({id:C.uid(),name:material.name,brand:material.brand||'',category:material.category||'',image:material.image||'',matches:[material.id],tags:[...(material.tags||[])]})}
 function pantryRemove(d,id){d.pantryItems=d.pantryItems.filter(i=>!i.matches.includes(id))}
 function readyCount(){return env.data.recipes.filter(r=>C.match(r,env.data).ready).length}
 function nav(){return `<header class="topbar"><button class="brand" data-view="recipes" aria-label="喝了么首页"><span class="brand-icon">${icon('glass')}</span><span><strong>喝了么</strong><small>一个贼牛逼的鸡尾酒记录工具</small></span></button><nav class="nav" aria-label="主要导航"><button data-view="recipes" class="${view==='recipes'?'active':''}" ${view==='recipes'?'aria-current="page"':''}>${icon('book')}酒谱<span class="nav-count">${env.data.recipes.length}</span></button><button data-view="pantry" class="${view==='pantry'?'active':''}" ${view==='pantry'?'aria-current="page"':''}>${icon('bottle')}我的酒柜</button><button data-view="settings" class="${view==='settings'?'active':''}" ${view==='settings'?'aria-current="page"':''}>${icon('cloud')}同步与数据</button></nav><button class="top-status" data-view="settings" title="查看保存与同步状态">${icon(syncBusy?'refresh':'check')}<span class="status-text">${esc(status())}</span></button></header>`}
 function render(){
   $('#app').innerHTML=nav()+`<main class="shell">${storageError?`<div class="notice" role="alert">${esc(storageError)}</div>`:''}${view==='recipes'?recipesPage():view==='pantry'?pantryPage():settingsPage()}<footer class="footer"><span>喝了么</span><span>个人酒谱 / v0.3.0</span></footer></main>`;
  if(view==='recipes')renderResults();if(view==='pantry')renderPantry();
 }
 function hierarchyName(entry,id){return entry?.[3]?.[id]||ingredient(id)?.name||id}
 function heading(eyebrow,title,desc,action=''){return `<div class="page-heading"><div><div class="eyebrow">${eyebrow}</div><h1>${title}</h1><p>${desc}</p></div>${action}</div>`}
 const selectOptions=(values,value,blank='请选择')=>`<option value="">${esc(blank)}</option>${values.map(v=>`<option value="${esc(v)}" ${v===value?'selected':''}>${esc(v)}</option>`).join('')}`;
 function filterGroup(label,key,values){return `<div class="filter-block"><h2 class="filter-title">${label}</h2><div class="chips">${values.map(v=>`<button class="chip ${filters[key].includes(v)?'active':''}" data-filter="${key}" data-value="${esc(v)}" aria-pressed="${filters[key].includes(v)}">${esc(v)}</button>`).join('')}</div></div>`}
 function recipesPage(){const values=k=>[...new Set([...(env.data.options[k==='sourceName'?'sources':k]||[]),...env.data.recipes.flatMap(r=>Array.isArray(r[k])?r[k]:[r[k]])].filter(Boolean))],radio=(v,l)=>`<label class="rail-radio"><input type="radio" name="availability" value="${v}" ${filters.availability===v?'checked':''}>${l}</label>`;return heading('THE RECIPE COLLECTION','我的酒谱','先收藏再说。记了等于喝了。',`<button class="btn primary" data-action="new">${icon('plus')}新增配方</button>`)+`<div class="workspace"><aside class="filter-rail ${filterOpen?'mobile-open':''}" aria-label="筛选酒谱"><div class="filter-head"><h2>筛选</h2><button class="link-button" data-action="reset-filters">重置</button></div><div class="filter-block bases"><h2 class="filter-title">基酒</h2><div class="base-filter-list">${[['','全部'],['金酒','金酒'],['朗姆酒','朗姆酒'],['伏特加','伏特加'],['龙舌兰','龙舌兰'],['威士忌','威士忌'],['白兰地','白兰地'],['其他烈酒','其他'],['多种烈酒','混合']].map(([v,l])=>`<button class="filter-choice ${filters.base===v?'active':''}" data-base-filter="${esc(v)}" aria-pressed="${filters.base===v}">${l}</button>`).join('')}</div></div>${filterGroup('风味标签','tags',values('tags'))}<div class="filter-block"><h2 class="filter-title">来源</h2><select id="source-filter">${selectOptions(values('sourceName'),filters.sourceName,'全部来源')}</select></div><div class="filter-block pantry-filter"><h2 class="filter-title">酒柜</h2><p class="filter-caption">根据我的原料筛选</p><div class="rail-radios">${radio('ready','全部拥有')}${radio('alcohol','仅根据酒精成分')}${radio('none','不筛选')}</div></div><label class="filter-choice favorite-choice"><input id="favorite-filter" type="checkbox" ${filters.favorite?'checked':''}>只看收藏</label></aside><section aria-label="酒谱列表"><div class="recipe-toolbar"><label class="search-field">${icon('search')}<span class="sr-only">搜索酒名、材料、风味或来源</span><input id="recipe-search" type="search" value="${esc(filters.q)}" placeholder="搜索酒名、材料、风味或来源…"></label><button class="btn small mobile-filter-toggle" data-action="toggle-filters" aria-expanded="${filterOpen}">${icon('filter')}筛选</button><div class="sort-options" aria-label="排序">${[['recent','最新'],['makeable','可制作'],['random','随机']].map(([v,l])=>`<button class="${sort===v?'active':''}" data-sort="${v}" aria-pressed="${sort===v}">${l}</button>`).join('')}</div></div><div id="recipe-results" class="result-grid" aria-live="polite"></div><div class="sample-banner">${icon('info')}<span>IBA 官网配方核对于 2026-09-21。可制作按基础配方判断，不要求装饰齐全，也不计算库存用量。</span></div></section></div>`}
 function recipePhoto(r){if(!r.image)return '';const src=r.image==='negroni'?'./assets/negroni.webp':'./assets/cocktails/'+r.image+'.webp';return src+'?v='+encodeURIComponent(window.MIX_PHOTOS?.[r.image]?.revision||'legacy')}
 function photo(r,cls=''){const src=recipePhoto(r);return src?`<img class="${cls}" src="${esc(src)}" alt="${esc(r.name)}，AI 配图" loading="lazy" width="640" height="800">`:`<div class="photo-placeholder ${cls}" aria-label="暂无配图，杯形：${esc(r.glass||'未指定')}">${glassIcon(r.glass)}<span>${esc(r.glass||'待补配图')}</span></div>`}
 function matchLabel(r){const m=C.match(r,env.data);return `<span class="match-label ${m.ready?'ready':m.missing.length===1?'one':''}">${icon(m.ready?'check':'minus')}${m.ready?(m.substitutions.length?'可制作 · 使用替代品':'材料齐全'):`还差 ${m.missing.length} 种材料`}</span>`}
 function renderResults(){let result=C.query(env.data,{...filters,availability:filters.availability==='ready'?'ready':'all'});if(filters.availability==='alcohol')result=result.filter(r=>r.ingredients.filter(x=>!x.optional&&C.ingredientPath(x.id,env.data).some(p=>p.id==='alcohol')).every(x=>C.have(x,env.data)));if(sort==='recent')result.sort((a,b)=>(b.createdAt||'').localeCompare(a.createdAt||''));if(sort==='makeable')result.sort((a,b)=>C.match(a,env.data).missing.length-C.match(b,env.data).missing.length||(b.createdAt||'').localeCompare(a.createdAt||''));if(sort==='random')result.sort(()=>Math.random()-.5);$('#recipe-results').innerHTML=result.length?result.map(r=>`<article class="recipe-card"><button class="card-visual" data-detail="${esc(r.id)}" aria-label="查看${esc(r.name)}">${photo(r,'catalog-photo')}</button><button class="card-main" data-detail="${esc(r.id)}"><span class="card-no">${esc(r.sourceName?'来源 · '+r.sourceName:'个人配方')}</span><h3>${esc(r.name)}</h3><p class="recipe-en">${esc(r.en)}</p><p class="card-ingredients">${r.ingredients.map(x=>esc(ingredientName(x.id))).join(' · ')}</p><div class="card-tags">${r.tags.slice(0,3).map(t=>`<span class="tag">${esc(t)}</span>`).join('')}${r.versions.length?`<span class="tag">${r.versions.length+1} 种比例</span>`:''}${r.parentId?'<span class="tag">变体</span>':''}</div></button><div class="card-bottom">${matchLabel(r)}<button class="icon-button ${env.data.favorites[r.id]?'is-favorite':''}" data-favorite="${esc(r.id)}" aria-label="收藏${esc(r.name)}" aria-pressed="${!!env.data.favorites[r.id]}">${icon('heart')}</button></div></article>`).join(''):`<div class="empty">${icon('search')}<h3>暂时没有符合的酒谱</h3><p>调整关键词或筛选条件试试。</p><button class="btn" data-action="reset-filters">清除筛选</button></div>`}
 function pantryDrawers(d){return d.ingredients.filter(i=>['alcohol','non-alcohol'].includes(i.id))}
 function pantryRoots(d,drawerId){return d.ingredients.filter(i=>i.parentId===drawerId&&i.kind==='type')}
 function pantryEntryTags(d,entry){
  const rootId=entry?.[0],builtIn=entry?.[2]||[],custom=d.ingredients.filter(i=>i.kind==='type'&&i.customized&&i.parentId===rootId).map(i=>i.id);
  return [...new Set([...builtIn,...custom])].map(id=>d.ingredients.find(i=>i.id===id)).filter(Boolean);
 }
 function pantryItemIcon(item,d){for(const id of item.matches||[]){const root=C.ingredientPath(id,d).find(i=>pantryIcons[i.id]);if(root)return pantryCategoryIcon(root.id)}return'bottle'}
 function pantryPage(){return heading('YOUR PERSONAL BAR','我的酒柜','- 你已经有 100 瓶鸡尾酒了！<br>- 不一样啦，不一样啦！',`<button class="btn primary" data-action="add-pantry-bottle">${icon('plus')}新增原料</button>`)+`<div class="pantry-mobile-toolbar"><button class="btn small mobile-filter-toggle" data-action="toggle-pantry-filters" aria-expanded="${pantryFilterOpen}">${icon('filter')}筛选分类</button></div><div class="pantry-workspace"><aside id="pantry-sidebar" class="pantry-sidebar ${pantryFilterOpen?'mobile-open':''}" aria-label="酒柜分类"></aside><section id="pantry-results" class="pantry-content" aria-live="polite"></section></div>`}
 function bottleCards(d,bottles){return bottles.map(i=>{const categoryIcon=pantryItemIcon(i,d);return `<button class="bottle-card" data-pantry-bottle="${esc(i.id)}">${i.image?`<img src="./assets/ingredients/${esc(i.image)}.webp" alt="" loading="lazy">`:`<div class="bottle-placeholder" data-category-icon="${categoryIcon}">${icon(categoryIcon)}</div>`}<div><h3>${esc(i.name)}</h3><p>${esc(i.brand||'通用材料')}</p><small>查看详情</small></div></button>`}).join('')}
 function renderPantry(){
  if(pantryDraft){pantryBrowser=new window.MaterialBrowser($('#pantry-directory'),pantryDraft,{mode:'pantry',isOwned:i=>pantryHas(pantryDraft,i.id),onToggle:(i,checked)=>{if(checked)pantryAdd(pantryDraft,i);else pantryRemove(pantryDraft,i.id);pantryBrowser.render()},onEdit:i=>openItem('pantry',i.id,{},()=>pantryBrowser.render()),onNew:defaults=>openItem('pantry','',defaults,i=>{pantryBrowser.data=pantryDraft;if(i.kind==='type')pantryBrowser.navigate(i.id);else pantryBrowser.render()})});return}
  const d=env.data,showAll=pantrySection==='all',entry=showAll?null:(pantryMenu(pantrySection)||pantryMenus[0]),selected=showAll?null:d.ingredients.find(i=>i.id===pantrySection);if(!showAll&&!selected){pantrySection=entry[1][0];selected=d.ingredients.find(i=>i.id===pantrySection)}
  $('#pantry-sidebar').innerHTML=`<div class="pantry-menu-group"><button class="pantry-menu-title pantry-all ${showAll?'active':''}" data-pantry-section="all">全部材料</button></div>${pantryMenus.map(entry=>{const [rootId,itemIds]=entry,root=d.ingredients.find(i=>i.id===rootId);if(!root)return'';const open=pantryOpen.has(rootId),categoryIcon=pantryCategoryIcon(rootId);return `<div class="pantry-menu-group"><button class="pantry-menu-title" data-pantry-group="${esc(rootId)}" aria-expanded="${open}"><span class="pantry-menu-label"><span class="pantry-category-icon">${icon(categoryIcon)}</span><span>${esc(hierarchyName(entry,rootId))}</span></span><span class="pantry-chevron ${open?'open':''}">${icon('chevron')}</span></button><div class="pantry-menu-items ${open?'open':''}">${itemIds.map(id=>d.ingredients.find(i=>i.id===id)).filter(Boolean).map(i=>`<button class="${i.id===pantrySection?'active':''}" data-pantry-section="${esc(i.id)}"><span>${esc(hierarchyName(entry,i.id))}</span></button>`).join('')}</div></div>`}).join('')}`;
  if(showAll){pantryTags=[];const owned=d.pantryItems;$('#pantry-results').innerHTML=`<div class="pantry-content-head"><div><div class="eyebrow">我的酒柜</div><h2>全部酒柜记录</h2></div></div><section class="pantry-bottles"><h3>已有材料 <span>${owned.length}</span></h3>${owned.length?`<div class="bottle-grid">${bottleCards(d,owned)}</div>`:`<div class="empty compact">${icon('bottle')}<h3>酒柜是空的</h3><p>酒谱需要的材料不会在这里冒充酒柜记录。请从分类中添加实际拥有的材料。</p></div>`}</section>`;return}
  if(!selected){$('#pantry-results').innerHTML='<div class="empty"><h3>还没有可用分类</h3></div>';return}
  const tags=pantryEntryTags(d,entry),available=new Set(tags.map(t=>t.id));pantryTags=pantryTags.filter(id=>available.has(id));
  const bottles=d.pantryItems.filter(i=>(i.matches.some(id=>C.satisfies(id,selected.id,d))||i.tags.includes(selected.id))&&pantryTags.every(id=>i.tags.includes(id)));
  $('#pantry-results').innerHTML=`<section class="pantry-tags"><div class="chips">${tags.map(t=>`<button class="chip ${pantryTags.includes(t.id)?'active':''}" data-pantry-tag="${esc(t.id)}" aria-pressed="${pantryTags.includes(t.id)}">${esc(t.name)}</button>`).join('')||'<span class="hint">暂无标签</span>'}</div></section><section class="pantry-bottles"><h3>${pantryTags.length?'筛选结果':'已有原料'} <span>${bottles.length}</span></h3>${bottles.length?`<div class="bottle-grid">${bottleCards(d,bottles)}</div>`:`<div class="empty compact">${icon('bottle')}<h3>${pantryTags.length?'没有同时满足这些标签的原料':'空'}</h3><p>${pantryTags.length?'减少一个标签再试试。':'点击页面右上角“新增原料”录入第一项。'}</p></div>`}</section>`;
 }
 function settingsPage(){const connected=!!(config.owner&&config.repo);return heading('KEEP YOUR RECIPES','同步与数据','可以保存在本地，也可以连接自己的 GitHub 仓库。其他方案暂时没做。')+`<div class="settings-layout"><div><section class="panel"><div class="row spread"><h2>连接 GitHub</h2><span class="sync-badge">${env.lastSync?'已连接':connected?'已配置':'尚未连接'}</span></div><p class="panel-intro">使用独立的私有仓库保存酒谱，所有设备读取同一份数据。</p><div class="connection-summary"><span class="connection-icon">${icon('cloud')}</span><div><strong>${connected?esc(config.owner+'/'+config.repo):'尚未配置数据仓库'}</strong><span>${connected?esc(config.path)+' · 自动同步已'+(config.auto?'开启':'关闭'):'用户名、仓库和访问令牌会在二级菜单中填写'}</span></div></div><div class="status-box">${esc(status())} · ${esc(dateText(env.lastSync))}</div><div class="row wrap connection-actions"><button class="btn primary" data-action="open-sync-settings">${connected?'管理连接':'连接 GitHub'} ${icon('arrow')}</button>${connected?`<button class="btn" data-action="sync-now" ${syncBusy?'disabled':''}>${icon('refresh')}${syncBusy?'正在同步':'手动同步'}</button>`:''}</div></section><p class="hint">手动同步会先让你选择上传本机数据或下载仓库数据，避免意外覆盖。未连接前，配方只保存在当前浏览器中。</p></div><div><section class="panel"><h2>导入与导出</h2><p class="panel-intro">没有仓库时，也可以先保存一份完整备份。</p><div class="backup-row"><div><strong>导出全部数据</strong><p>配方、材料、酒柜和收藏</p></div><button class="btn small" data-action="export">${icon('download')}导出</button></div><div class="backup-row"><div><strong>导入已有数据</strong><p>合并记录，遇到冲突会询问</p></div><button class="btn small" data-action="import">${icon('upload')}导入</button></div><input id="import-file" type="file" accept=".json,application/json" hidden></section><section class="panel"><h2>仓库准备好后</h2><ol class="steps-list"><li>创建一个<strong>私有仓库</strong>，勾选添加 README，先保留空白即可。</li><li>创建仅限该仓库的 Fine-grained Token，授予 Contents 读写权限。</li><li>打开“连接 GitHub”二级菜单填写信息并保存。</li></ol><a class="link-button" href="https://github.com/settings/personal-access-tokens/new" target="_blank" rel="noopener noreferrer">打开 GitHub 令牌设置 ${icon('arrow')}</a></section><section class="panel danger-zone"><h2>初版示例酒谱</h2><p class="hint">移除内置示例，保留你新增或编辑过的配方，以及酒柜材料。</p><button class="link-button" data-action="remove-samples">移除未修改的示例</button></section><section class="panel danger-zone"><h2>本地数据</h2><p class="hint">清空此浏览器中的配方、酒柜、收藏和同步设置。建议先导出备份。</p><button class="btn danger" data-action="clear-local-data">清空本地数据</button></section></div></div>`}
 function openSyncSettings(){const el=$('#sync-dialog');el.innerHTML=`<form id="sync-form"><div class="dialog-top"><span>GitHub 同步设置</span><button type="button" class="icon-button" data-close="sync-dialog" aria-label="关闭同步设置">${icon('close')}</button></div><div class="editor-body"><div class="status-box">${esc(status())} · ${esc(dateText(env.lastSync))}<br><span class="muted">自动同步已${config.auto?'开启':'关闭'}</span>${config.repo?`<br><span class="muted">${esc(config.owner+'/'+config.repo+'/'+config.path)}</span>`:''}</div><div class="field-row"><label class="field">GitHub 用户名<input name="owner" autocomplete="off" placeholder="例如 vio" value="${esc(config.owner)}" required></label><label class="field">私有数据仓库名<input name="repo" autocomplete="off" placeholder="例如 mixbook-data" value="${esc(config.repo)}" required></label></div><div class="field-row"><label class="field">分支<input name="branch" placeholder="留空使用默认分支" value="${esc(config.branch)}"></label><label class="field">数据文件<input name="path" placeholder="mixbook.json" value="${esc(config.path)}" required></label></div><label class="field">访问令牌（Fine-grained Token）<input name="token" type="password" autocomplete="off" spellcheck="false" placeholder="仅授予该仓库的 Contents 读写权限" value="${esc(token)}"></label><label class="check-line"><input name="remember" type="checkbox" ${remember?'checked':''}><span>在此设备记住令牌<br><span class="hint">只在自己的设备上启用；令牌不会写入仓库或导出文件。</span></span></label><label class="switch-line"><span><strong>前台自动同步</strong><span class="hint">打开应用、保存后和前台每 60 秒检查；更改开关后需保存设置才会生效。</span></span><span class="switch"><input name="auto" type="checkbox" role="switch" ${config.auto?'checked':''}><span aria-hidden="true"></span></span></label><div id="sync-error" class="form-error" role="alert"></div></div><div class="dialog-actions">${config.repo?'<button class="btn danger left" type="button" data-action="disconnect">断开连接</button>':''}<button class="btn" type="button" data-close="sync-dialog">取消</button><button class="btn primary" type="submit" ${syncBusy?'disabled':''}>保存同步设置</button></div></form>`;showDialog(el)}
 function showDialog(el){dialogFocus.set(el,document.activeElement);el.showModal()}
 function closeDialog(el){el.close();dialogFocus.get(el)?.focus?.()}
 function safeLink(v){try{const u=new URL(v);return u.protocol==='https:'?u.href:''}catch{return ''}}
 function detailRows(rows){return rows.map(x=>{const owned=C.matches(x,env.data),sub=owned.length&&!C.have({...x,alternatives:[]},env.data);return `<div class="ingredient-row ${owned.length?'have':''}">${icon(owned.length?'check':'minus')}<span>${esc(recipeIngredientName(x.id))}${x.optional?' <small>可选</small>':''}${x.alternatives?.length?`<small class="owned-match">可替代：${x.alternatives.map(id=>esc(ingredientName(id))).join('、')}</small>`:''}${owned.length?`<small class="owned-match">${sub?'使用替代品':'酒柜'}：${owned.map(i=>esc(i.name)).join('、')}</small>`:''}</span><span class="amount">${esc(x.amount)} ${esc(x.unit)}</span></div>`}).join('')}
 function openDetail(id,version=''){const base=env.data.recipes.find(r=>r.id===id);if(!base)return;const v=base.versions.find(x=>x.id===version),r=v?{...base,...v,id:base.id}:base;selectedVersion=v?.id||'';const parent=env.data.recipes.find(x=>x.id===base.parentId),children=env.data.recipes.filter(x=>x.parentId===id);const el=$('#detail-dialog');el.dataset.recipe=id;el.innerHTML=`<div class="dialog-top"><span>配方详情</span><button class="icon-button" data-close="detail-dialog" aria-label="关闭详情">${icon('close')}</button></div><div class="detail-body"><div class="detail-hero"><div><div class="eyebrow">${esc([base.base,base.method].filter(Boolean).join(' / '))}</div><h2>${esc(base.name)}</h2><p class="recipe-en">${esc(base.en)}</p><div class="detail-meta"><span>${esc(base.glass)}</span>${base.tags.map(t=>`<span class="tag">${esc(t)}</span>`).join('')}</div><p class="recipe-source">来源：${esc(base.sourceName||'未填写')}</p></div>${photo(base,'detail-photo')}</div><div class="version-tabs" aria-label="比例版本"><button class="chip ${!v?'active':''}" data-version="" data-recipe="${esc(id)}" aria-pressed="${!v}">${base.customized?'基础比例 · 已调整':'基础比例'}</button>${base.versions.map(x=>`<button class="chip ${v?.id===x.id?'active':''}" data-version="${esc(x.id)}" data-recipe="${esc(id)}" aria-pressed="${v?.id===x.id}">${esc(x.name)}</button>`).join('')}<button class="chip" data-add-version="${esc(id)}">${icon('plus')}添加比例</button></div>${v?`<p class="hint">${esc(v.name)}${v.author?' · '+esc(v.author):''}<button class="link-button" data-edit-version="${esc(v.id)}" data-recipe="${esc(id)}">编辑此比例</button></p>`:''}<div class="row wrap detail-status">${matchLabel(r)}<span class="hint">装饰单独列出，不影响可制作判断。</span></div><div class="detail-columns"><section><h3>调酒材料</h3>${detailRows(r.ingredients)}<h3 class="garnish-heading">装饰 Garnish</h3>${r.garnishes.length?detailRows(r.garnishes):'<p class="hint">无需装饰</p>'}</section><section><h3>调制步骤</h3><ol class="method-steps">${r.steps.map(s=>`<li>${esc(s)}</li>`).join('')}</ol></section></div>${r.notes?`<section class="notes-box"><h3>${v?'此比例笔记':'配方笔记'}</h3><p>${esc(r.notes)}</p></section>`:''}${window.MIX_PHOTOS?.[base.image]?.note?`<p class="hint photo-note">配图说明：${esc(window.MIX_PHOTOS[base.image].note)}</p>`:''}<section class="related-recipes"><h3>本体与变体</h3>${parent?`<p class="hint">本体：<button class="link-button" data-detail="${esc(parent.id)}">${esc(parent.name)}</button></p>`:''}<div class="chips">${children.map(x=>`<button class="chip" data-detail="${esc(x.id)}">${esc(x.name)}</button>`).join('')}<button class="chip" data-variant="${esc(id)}">${icon('plus')}从这杯创建变体</button></div></section><p class="source-line">${safeLink(base.source)?`配方参考：<a href="${esc(safeLink(base.source))}" target="_blank" rel="noopener noreferrer">查看原文</a>`:esc(base.source||'个人配方')}${recipePhoto(base)?' · AI 配图仅作示意，以配方文字为准。':''}</p></div><div class="dialog-actions"><button class="icon-button left ${env.data.favorites[id]?'is-favorite':''}" data-favorite="${esc(id)}" aria-label="收藏此配方">${icon('heart')}</button><button class="btn" data-duplicate="${esc(id)}">复制</button><button class="btn primary" data-edit="${esc(id)}">编辑配方</button></div>`;if(!el.open)showDialog(el)}
 function optionsFor(k){return [...new Set([...editorDraft.options[k],...env.data.recipes.flatMap(r=>k==='glasses'?[r.glass]:k==='sources'?[r.sourceName]:r[k]||[])])].filter(Boolean)}
 function glassOptions(value){return {current:value,values:[...new Set([...(window.MIX_TAXONOMY?.recipe?.glasses||editorDraft.options.glasses),value])].filter(Boolean)}}
 function optionPicker(k,label,value){const single=k==='glasses'||k==='sources',glass=k==='glasses'?glassOptions(value):null,choice=v=>k==='glasses'?`<label class="glass-option" title="${esc(v)}"><input type="radio" name="glasses" value="${esc(v)}" ${glass.current===v?'checked':''}><span class="glass-option-icon">${glassIcon(v)}</span><span class="glass-option-name">${esc(v)}</span></label>`:`<label class="chip pick-chip"><input type="${single?'radio':'checkbox'}" name="${k}" value="${esc(v)}" ${(single?value===v:value.includes(v))?'checked':''}>${esc(v||'未填写')}</label>`;return `<fieldset class="option-picker ${k==='glasses'?'glass-picker':''}" data-options="${k}"><legend>${label}</legend>${k==='glasses'?'<p class="field-help">相近规格已合并，按杯身轮廓选择即可。</p>':''}<div class="chips">${[...(k==='sources'?['']:[]),...(glass?glass.values:optionsFor(k))].map(choice).join('')}<button class="chip add-chip" type="button" data-reveal-option="${k}">${icon('plus')}新增</button><span class="inline-option-add" hidden><input type="text" maxlength="${k==='sources'?200:60}" data-new-option="${k}" placeholder="新增${label}" aria-label="新增${label}"><button class="chip add-chip" type="button" data-add-option="${k}">添加</button></span></div></fieldset>`}
 function variantPicker(r){const variant=!!r.parentId,parents=env.data.recipes.filter(x=>x.id!==editorId);return `<fieldset class="variant-picker"><legend>变体</legend><div class="variant-controls"><label><input type="radio" name="recipeKind" value="independent" ${variant?'':'checked'}><span>独立配方</span></label><label><input type="radio" name="recipeKind" value="variant" ${variant?'checked':''}><span>变体</span></label><input name="parentId" list="recipe-parent-options" value="${esc(parents.find(x=>x.id===r.parentId)?.name||'')}" placeholder="输入本体名称搜索" ${variant?'':'disabled'} autocomplete="off"><datalist id="recipe-parent-options">${parents.map(x=>`<option value="${esc(x.name)}"></option>`).join('')}</datalist></div></fieldset>`}
 function ingredientOptions(){return `<datalist id="unit-options">${['ml','吧匙','茶匙','汤匙','dash','滴','片','枝','颗','撮','块','份'].map(x=>`<option>${x}</option>`).join('')}</datalist>`}
 function editorRow(x={id:'',amount:'',unit:'ml',optional:false}){const i=editorDraft.ingredients.find(i=>i.id===x.id),alts=x.alternatives||[],pickerName=`recipeMaterial-${C.uid()}`,display=i?recipeIngredientName(i.id,editorDraft):'';return `<div class="ingredient-edit-row"><input type="hidden" name="ingredientId" value="${esc(i?.id||'')}"><div class="editor-row"><details class="type-dropdown recipe-material-dropdown"><summary><input type="text" data-type-search data-selected-display="${esc(display)}" value="${esc(display)}" placeholder="选择原料" autocomplete="off" aria-label="搜索或选择原料"></summary>${typeCascade(editorDraft,i?.id||'',pickerName,'data-recipe-material')}</details><input type="text" inputmode="decimal" autocomplete="off" name="amount" aria-label="用量" value="${esc(x.amount)}" placeholder="30" maxlength="40" required><input type="text" autocomplete="off" name="unit" aria-label="单位" value="${esc(x.unit)}" maxlength="30"><label class="optional-check"><input name="optional" type="checkbox" ${x.optional?'checked':''}>可选</label><button type="button" class="icon-button" data-action="remove-row" aria-label="移除此项">${icon('close')}</button></div><div class="specific-material"><button type="button" class="link-button" data-action="reveal-specific" ${i?'':'disabled'}>＋ 添加具体原料</button><div class="specific-material-editor" hidden><div class="specific-kind"><label><input type="radio" name="specificKind-${pickerName}" value="name" checked>名称</label><label><input type="radio" name="specificKind-${pickerName}" value="tag">标签</label></div><input type="text" data-specific-name maxlength="100" placeholder="输入名称"><button type="button" class="btn small" data-action="confirm-specific">确认</button></div></div><div class="row-alternative"><div class="chips alternative-list">${alts.map(id=>`<span class="chip" data-alternative="${esc(id)}">${esc(recipeIngredientName(id,editorDraft))}<button type="button" data-remove-alternative aria-label="移除替代品">×</button></span>`).join('')}</div><button type="button" class="link-button alternative-add" data-add-alternative>＋ 添加替代品</button></div></div>`}
 function readEditorRow(el){return{id:el.querySelector('[name=ingredientId]').value,amount:el.querySelector('[name=amount]').value.trim(),unit:el.querySelector('[name=unit]').value.trim(),optional:el.querySelector('[name=optional]').checked,alternatives:[...el.querySelectorAll('[data-alternative]')].map(x=>x.dataset.alternative)}}
 function openMaterial(onSelect,initialId=''){
  const d=editorDraft,initial=d.ingredients.find(i=>i.id===initialId),el=$('#material-dialog');
  el.innerHTML='<div class="dialog-top material-dialog-top"><button type="button" class="icon-button back-button" data-close="material-dialog" aria-label="返回配方编辑">'+icon('arrow')+'</button><span>选择原料</span></div><div class="editor-body" id="recipe-material-browser"></div>';
  const choose=i=>{onSelect(i);closeDialog(el)};
  pickerBrowser=new window.MaterialBrowser($('#recipe-material-browser'),d,{mode:'recipe',hierarchy:pantryMenus,selectedId:initialId,parentId:'',group:initial?.category||'',onSelect:choose,onQuickNew:(kind,name,parentId)=>{const existing=d.ingredients.find(i=>C.norm(i.name)===C.norm(name));if(existing){choose(existing);return}const parent=d.ingredients.find(i=>i.id===parentId);const i={id:C.uid(),name,kind,parentId,brand:'',category:parent?.category||'',aliases:[],tags:[],image:'',matchParent:true,customized:true};d.ingredients.push(i);choose(i)}});
  showDialog(el);
 }
 function recipeChoice(name,label,values,value,optional=false){return `<fieldset class="option-picker recipe-choice"><legend>${label}</legend><div class="chips">${optional?`<label class="chip pick-chip"><input type="radio" name="${name}" value="" ${value?'':'checked'}>未指定</label>`:''}${values.map(v=>`<label class="chip pick-chip"><input type="radio" name="${name}" value="${esc(v)}" ${value===v?'checked':''}>${esc(v)}</label>`).join('')}</div></fieldset>`}
 function openEditor(id=null,mode='recipe',version=''){if(syncBusy){toast('请等待同步完成再编辑。');return}editorDraft=C.clone(env.data);const old=env.data.recipes.find(r=>r.id===id);editorMode=mode;editorId=(mode==='duplicate'||mode==='variant')?null:id;editorVersion=version;let r=C.clone(old||{name:'',en:'',base:'',method:'摇和',glass:'鸡尾酒杯',tags:[],ingredients:[],garnishes:[],steps:[''],notes:'',source:'',sourceName:'',parentId:'',versions:[],image:''});if(mode==='version')r={...r,...(old.versions.find(v=>v.id===version)||{name:'',author:'',notes:''})};if(mode==='variant'){r.name='';r.en='';r.parentId=id}if(mode==='duplicate')r.name+=' · 副本';if($('#detail-dialog').open)closeDialog($('#detail-dialog'));const names=mode==='version'?`<label class="field"><span><span class="required">*</span>比例名称</span><input name="name" required maxlength="100" value="${esc(r.name)}" placeholder="例如：偏金酒 45 / 30 / 30"></label><label class="field">调酒师 / 记录者（可选）<input name="author" value="${esc(r.author||'')}" maxlength="100"></label>`:`<div class="field-row recipe-name-row"><label class="field"><span><span class="required">*</span>酒名</span><input name="name" required maxlength="100" value="${esc(r.name)}" placeholder="鸡尾酒名称"></label><label class="field"><span>英文名</span><input name="en" maxlength="100" value="${esc(r.en)}"></label></div>`;const materials=`<h3 class="editor-section-title"><span class="required">*</span>原料</h3><div id="editor-ingredients">${(r.ingredients.length?r.ingredients:[{}]).map(x=>editorRow({amount:'',unit:'ml',optional:false,...x})).join('')}</div><div class="row wrap"><button type="button" class="link-button" data-add-row="ingredients">${icon('plus')}添加原料</button></div><h3 class="editor-section-title">装饰</h3><div id="editor-garnishes">${r.garnishes.map(editorRow).join('')}</div><button type="button" class="link-button" data-add-row="garnishes">${icon('plus')}添加装饰</button>`;const choices=mode==='version'?'':recipeChoice('base','基酒',['金酒','朗姆酒','伏特加','龙舌兰','威士忌','白兰地','其他烈酒','多种烈酒'],r.base,true)+recipeChoice('method','调制方式',['摇和','搅拌','直调','捣压','搅打','旋调'],r.method);const el=$('#editor-dialog');el.innerHTML=`<form id="recipe-form"><div class="dialog-top"><span>${mode==='version'?'比例版本':mode==='variant'?'创建变体':editorId?'编辑配方':'新增配方'}</span><button type="button" class="icon-button" data-close="editor-dialog" aria-label="关闭编辑">${icon('close')}</button></div><div class="editor-body"><div id="editor-catalog">${ingredientOptions()}</div>${names}${materials}${choices}<label class="field"><span><span class="required">*</span>调制步骤</span><textarea name="steps" required rows="4" placeholder="每行一个步骤">${esc(r.steps.join('\n'))}</textarea></label>${mode==='version'?'':optionPicker('glasses','杯形',r.glass)+optionPicker('tags','风味标签',r.tags)+variantPicker(r)+optionPicker('sources','来源',r.sourceName)}<label class="field">${mode==='version'?'此比例笔记':'配方笔记'}<textarea name="notes" maxlength="10000">${esc(r.notes)}</textarea></label>${mode==='version'?'':`<label class="field">来源链接 / 备注（可选）<input name="source" maxlength="2000" value="${esc(r.source)}" placeholder="网页链接、书名页码等"></label>`}<div id="editor-error" class="form-error" role="alert"></div></div><div class="dialog-actions">${editorId&&mode==='recipe'?`<button type="button" class="btn danger left" data-delete="${esc(editorId)}">删除</button>`:''}${mode==='version'&&version?`<button type="button" class="btn danger left" data-delete-version="${esc(version)}">删除比例</button>`:''}<button type="button" class="btn" data-close="editor-dialog">取消</button><button class="btn primary" type="submit">保存${mode==='version'?'比例':'配方'}</button></div></form>`;showDialog(el)}
 async function saveRecipe(form){try{const f=new FormData(form),name=String(f.get('name')||'').trim(),steps=String(f.get('steps')||'').split('\n').map(x=>x.trim()).filter(Boolean);if(!name||!steps.length)throw Error('请填写名称和调制步骤。');function collect(section){return [...form.querySelectorAll('#editor-'+section+' .ingredient-edit-row')].map(el=>{const row=readEditorRow(el);if(!row.id||!row.amount)throw Error('每项材料或装饰都需要选择材料并填写用量。');if(!editorDraft.ingredients.some(i=>i.id===row.id))throw Error('材料引用已失效，请重新选择。');return row})}const ingredients=collect('ingredients'),garnishes=collect('garnishes');if(!ingredients.length)throw Error('至少需要一种原料。');const id=editorId||C.uid(),versionId=editorVersion||C.uid();const ok=await update(d=>{d.ingredients=C.clone(editorDraft.ingredients);d.options=C.clone(editorDraft.options);const old=d.recipes.find(r=>r.id===id);if(editorMode==='version'){const v={id:versionId,name,author:String(f.get('author')||'').trim(),notes:String(f.get('notes')||'').trim(),ingredients,garnishes,steps};const index=old.versions.findIndex(x=>x.id===versionId);if(index<0)old.versions.push(v);else old.versions[index]=v;old.updatedAt=C.now();return}const r={...(old||{}),id,name,en:String(f.get('en')||'').trim(),base:String(f.get('base')||'').trim(),method:String(f.get('method')||'摇和'),glass:String(f.get('glasses')||''),tags:f.getAll('tags').map(String),notes:String(f.get('notes')||'').trim(),source:String(f.get('source')||'').trim(),sourceName:String(f.get('sources')||'').trim(),parentId:f.get('recipeKind')==='variant'?(env.data.recipes.find(x=>x.id===String(f.get('parentId')||'')||C.norm(x.name)===C.norm(String(f.get('parentId')||'')))?.id||''):'',ingredients,garnishes,steps,versions:old?.versions||[],catalog:old?.catalog||false,customized:!!old?.catalog,sample:false,image:old?.image||'',createdAt:old?.createdAt||C.now(),updatedAt:C.now()};if(old)d.recipes[d.recipes.indexOf(old)]=r;else d.recipes.unshift(r)});if(ok){closeDialog($('#editor-dialog'));editorDraft=null;render();openDetail(id,editorMode==='version'?versionId:'');scheduleSync();toast('已保存。')}}catch(e){$('#editor-error').textContent=e.message}}
 function pantryContext(rootId=pantrySection){const d=env.data,entry=pantryMenu(rootId),root=d.ingredients.find(i=>i.id===(entry?.[0]||rootId)),tags=pantryEntryTags(d,entry);return{root,tags}}
 function openPantryBottle(id){const i=env.data.pantryItems.find(x=>x.id===id);if(!i)return;itemId=id;const names=(i.tags||[]).map(x=>ingredientName(x)),el=$('#item-dialog'),categoryIcon=pantryItemIcon(i,env.data);el.innerHTML=`<div class="dialog-top"><span>酒柜记录</span><button class="icon-button" data-close="item-dialog" aria-label="关闭">${icon('close')}</button></div><div class="editor-body bottle-detail"><div class="bottle-detail-icon" data-category-icon="${categoryIcon}">${icon(categoryIcon)}</div><div class="eyebrow">${esc(i.brand||'通用材料')}</div><h2>${esc(i.name)}</h2><div class="chips">${names.map(x=>`<span class="chip">${esc(x)}</span>`).join('')||'<span class="hint">未设置标签</span>'}</div></div><div class="dialog-actions"><button class="btn danger left" data-action="delete-pantry-bottle">删除酒柜记录</button><button class="btn" data-close="item-dialog">关闭</button><button class="btn primary" data-edit-pantry-bottle="${esc(id)}">${icon('edit')}修改</button></div>`;showDialog(el)}
 function typeCascade(d,selected='',inputName='rootId',inputAttr=''){const selectedEntry=pantryMenu(selected)||pantryMenus[0],active=selectedEntry?.[0];return `<div class="type-cascade"><div class="type-drawers" role="tablist" aria-label="一级分类">${pantryMenus.map(entry=>`<button type="button" role="tab" data-type-drawer="${esc(entry[0])}" ${inputAttr?`data-recipe-material-root="${esc(entry[0])}"`:''} class="${entry[0]===active?'active':''}" aria-selected="${entry[0]===active}"><span>${esc(hierarchyName(entry,entry[0]))}</span><span aria-hidden="true">›</span></button>`).join('')}</div><div class="type-options">${pantryMenus.map(entry=>`<div role="tabpanel" data-type-options="${esc(entry[0])}" ${entry[0]===active?'':'hidden'}>${entry[1].map(id=>d.ingredients.find(i=>i.id===id)).filter(Boolean).map(i=>`<label data-type-name="${esc(C.norm(hierarchyName(entry,i.id)))}"><input type="radio" name="${inputName}" value="${esc(i.id)}" ${inputAttr} ${i.id===selected?'checked':''}><span>${esc(hierarchyName(entry,i.id))}</span></label>`).join('')}</div>`).join('')}</div></div>`}
 function pantryTypePicker(d,roots,selected,showSelection=false){const selectedEntry=pantryMenu(selected)||pantryMenus[0],display=showSelection&&selected?`${hierarchyName(selectedEntry,selectedEntry[0])} · ${hierarchyName(selectedEntry,selected)}`:'';return `<fieldset class="pantry-type-picker"><legend><span class="required">*</span>原料类型</legend><details class="type-dropdown"><summary><input type="text" data-type-search data-selected-display="${esc(display)}" value="${esc(display)}" placeholder="选择原料分类" autocomplete="off" aria-label="搜索或选择原料分类"></summary>${typeCascade(d,selected)}</details></fieldset>`}
 function openPantryBottleForm(id='',rootId='',draft=null){const d=env.data,old=d.pantryItems.find(i=>i.id===id),matched=old&&d.ingredients.find(i=>old.matches.includes(i.id)),roots=pantryMenus.flatMap(entry=>entry[1]).map(id=>d.ingredients.find(i=>i.id===id)).filter(Boolean),chosen=rootId||(!['all',''].includes(pantrySection)?pantrySection:'')||(matched?.kind==='product'?C.ingredientPath(matched.id,d).find(x=>pantryMenu(x.id))?.id:matched?.id)||roots[0]?.id,{root,tags}=pantryContext(chosen);if(!root)return;const i=draft||old||{name:'',brand:'',tags:[],newTags:[]};itemId=id;const newTags=i.newTags||[],el=$('#item-dialog');el.innerHTML=`<form id="pantry-bottle-form"><div class="dialog-top"><span>${id?'修改原料':'新增原料'}</span><button type="button" class="icon-button" data-close="item-dialog" aria-label="关闭">${icon('close')}</button></div><div class="editor-body pantry-item-form"><label class="field"><span><span class="required">*</span>原料名称</span><input name="name" required maxlength="100" value="${esc(i.name)}" placeholder="请输入"></label>${pantryTypePicker(d,roots,chosen,!!(rootId||old))}<label class="field"><span>品牌</span><input name="brand" maxlength="100" value="${esc(i.brand)}" placeholder="请输入"></label><fieldset class="option-picker pantry-tag-picker"><legend>标签（可多选）</legend><div class="chips">${tags.map(t=>`<label class="chip pick-chip"><input type="checkbox" name="materialTags" value="${esc(t.id)}" ${(i.tags||[]).includes(t.id)?'checked':''}>${esc(t.name)}</label>`).join('')}${newTags.map((t,n)=>`<label class="chip pick-chip new-tag"><input type="checkbox" name="newMaterialTags" value="${esc(t)}" checked>${esc(t)}<button type="button" data-remove-new-tag="${n}" aria-label="移除${esc(t)}">×</button></label>`).join('')}<button type="button" class="chip add-chip" data-action="reveal-inline-pantry-tag">${icon('plus')}新增</button><span class="inline-option-add" hidden><input name="newTagName" maxlength="100" placeholder="标签名称" aria-label="标签名称"><button type="button" class="chip add-chip" data-action="add-inline-pantry-tag">添加</button></span></div></fieldset><div id="item-error" class="form-error" role="alert"></div></div><div class="dialog-actions">${id?'<button type="button" class="btn danger left" data-action="delete-pantry-bottle">删除</button>':''}<button type="button" class="btn" data-action="reset-pantry-bottle">重置</button><button class="btn primary" type="submit">保存</button></div></form>`;if(!el.open)showDialog(el)}
 function pantryBottleDraft(form){const f=new FormData(form);return{name:String(f.get('name')||''),brand:String(f.get('brand')||''),tags:f.getAll('materialTags').map(String),newTags:f.getAll('newMaterialTags').map(String)}}
 function openPantryTags(){const{root,tags}=pantryContext();if(!root)return;const el=$('#item-dialog'),row=t=>`<div class="tag-edit-row" data-tag-row="${esc(t?.id||'')}"><input name="tagName" maxlength="100" value="${esc(t?.name||'')}" placeholder="标签名称"><button type="button" class="icon-button" data-remove-tag aria-label="删除标签">${icon('trash')}</button></div>`;el.innerHTML=`<form id="pantry-tags-form"><div class="dialog-top"><span>编辑标签 · ${esc(root.name)}</span><button type="button" class="icon-button" data-close="item-dialog" aria-label="关闭">${icon('close')}</button></div><div class="editor-body"><p class="hint">这里只管理 ${esc(root.name)} 的标签。例如朗姆酒可以同时使用“古巴朗姆”和“白朗姆”。</p><div id="pantry-tag-rows">${tags.map(row).join('')}</div><button type="button" class="link-button" data-add-tag>${icon('plus')}新增标签</button><div id="item-error" class="form-error" role="alert"></div></div><div class="dialog-actions"><button type="button" class="btn" data-close="item-dialog">取消</button><button class="btn primary" type="submit">保存</button></div></form>`;showDialog(el)}
 async function savePantryBottle(form){const f=new FormData(form),selectedId=String(f.get('rootId')||pantrySection),{root}=pantryContext(selectedId),selected=env.data.ingredients.find(i=>i.id===selectedId)||root,name=String(f.get('name')||'').trim(),brand=String(f.get('brand')||'').trim(),newNames=[...new Set(f.getAll('newMaterialTags').map(x=>String(x).trim()).filter(Boolean))];if(!name){$('#item-error').textContent='请填写原料名称。';return}const ok=await update(d=>{const created=newNames.map(tagName=>{const existing=d.ingredients.find(x=>x.parentId===root.id&&x.kind==='type'&&C.norm(x.name)===C.norm(tagName));if(existing)return existing.id;const id=C.uid();d.ingredients.push({id,name:tagName,kind:'type',parentId:root.id,category:root.category,brand:'',aliases:[],tags:[],image:'',matchParent:true,customized:true});return id}),id=itemId||C.uid(),old=d.pantryItems.find(i=>i.id===id),named=d.ingredients.find(i=>C.norm(i.name)===C.norm(name)),namedMatchesSelection=named&&C.ingredientPath(named.id,d).some(x=>x.id===selected.id),i={id,name,brand,category:selected.category,image:old?.image||'',matches:[namedMatchesSelection?named.id:selected.id],tags:[...new Set([...f.getAll('materialTags').map(String),...created])]};if(old)d.pantryItems[d.pantryItems.indexOf(old)]=i;else d.pantryItems.push(i)});if(ok){closeDialog($('#item-dialog'));renderPantry();updateHeader();scheduleSync();toast('酒柜记录已保存。')}}
 async function savePantryTags(form){const{root,tags}=pantryContext(),rows=[...form.querySelectorAll('[data-tag-row]')],kept=new Set(rows.map(r=>r.dataset.tagRow).filter(Boolean)),removed=tags.filter(t=>!kept.has(t.id));const used=new Set(env.data.recipes.flatMap(r=>[...r.ingredients,...r.garnishes,...r.versions.flatMap(v=>[...v.ingredients,...v.garnishes])]).flatMap(r=>[r.id,...(r.alternatives||[])]));const values=rows.map(r=>({id:r.dataset.tagRow||C.uid(),name:r.querySelector('[name=tagName]').value.trim()})).filter(x=>x.name);if(new Set(values.map(x=>C.norm(x.name))).size!==values.length){$('#item-error').textContent='标签名称不能重复。';return}const ok=await update(d=>{const removeIds=new Set(removed.map(x=>x.id));d.ingredients=d.ingredients.filter(i=>!removeIds.has(i.id)||used.has(i.id));for(const i of d.ingredients){i.tags=i.tags.filter(id=>!removeIds.has(id));if(removeIds.has(i.parentId))i.parentId=root.id;if(removeIds.has(i.id)&&used.has(i.id))i.parentId=''}for(const item of d.pantryItems){item.tags=item.tags.filter(id=>!removeIds.has(id));item.matches=item.matches.map(id=>removeIds.has(id)?root.id:id)}for(const v of values){const old=d.ingredients.find(i=>i.id===v.id);if(old){old.name=v.name;old.customized=true}else d.ingredients.push({id:v.id,name:v.name,kind:'type',parentId:root.id,category:root.category,brand:'',aliases:[],tags:[],image:'',matchParent:true,customized:true})}});if(ok){closeDialog($('#item-dialog'));renderPantry();updateHeader();scheduleSync();toast('标签已保存。')}}
 function openItem(context,id='',defaults={},onSave=null){
  itemContext=context;itemId=id;itemReturn=onSave;
  const d=context==='pantry'?pantryDraft:editorDraft,i=d.ingredients.find(x=>x.id===id)||{name:defaults.name||'',category:defaults.category||'基酒',parentId:defaults.parentId||'',brand:'',kind:defaults.kind||'product',aliases:[],tags:[],matchParent:true};
  const parents=d.ingredients.filter(x=>x.kind==='type'&&!C.ingredientPath(x.id,d).some(p=>p.id===id));
  const baseRoot=C.ingredientPath(i.parentId,d).find(x=>x.parentId==='spirit')||d.ingredients.find(x=>x.id===i.parentId&&x.parentId==='spirit');
  const tagChoices=baseRoot?d.ingredients.filter(x=>x.kind==='type'&&x.id!==baseRoot.id&&C.ingredientPath(x.id,d).some(p=>p.id===baseRoot.id)):[];
  const el=$('#item-dialog');
  el.innerHTML=`<form id="item-form"><div class="dialog-top"><span>${i.kind==='type'?'编辑标签':id?'修改酒款详情':'添加酒'}</span><button type="button" class="icon-button" data-close="item-dialog" aria-label="关闭材料编辑">${icon('close')}</button></div><div class="editor-body"><label class="field">名称 *<input name="name" required maxlength="100" value="${esc(i.name)}" placeholder="例如：哈瓦那俱乐部 3 年"></label><label class="field">记录类型<select name="kind" ${id?'disabled':''}><option value="product" ${i.kind==='product'?'selected':''}>具体酒款 / 产品</option><option value="type" ${i.kind==='type'?'selected':''}>标签 / 通用材料</option></select></label><label class="field">所属大类<select name="parentId"><option value="">独立材料 / 产品</option>${parents.map(x=>`<option value="${esc(x.id)}" ${i.parentId===x.id?'selected':''}>${esc(C.ingredientPath(x.id,d).filter(p=>p.id!=='spirit').map(p=>p.name).join(' › ')||x.name)}</option>`).join('')}</select></label><label class="field" data-brand-field ${i.kind==='type'?'hidden':''}>品牌（可选）<input name="brand" maxlength="100" value="${esc(i.brand)}"></label>${i.kind==='product'&&tagChoices.length?`<fieldset class="option-picker"><legend>酒款标签（可多选）</legend><p class="hint">例如同时选择“古巴朗姆”和“白朗姆”。</p><div class="chips">${tagChoices.map(t=>`<label class="chip pick-chip"><input type="checkbox" name="materialTags" value="${esc(t.id)}" ${(i.tags||[]).includes(t.id)?'checked':''}>${esc(t.name)}</label>`).join('')}</div></fieldset>`:''}<label class="field">浏览分组 *<input name="category" list="category-options" maxlength="50" required value="${esc(i.category)}"><datalist id="category-options">${[...new Set(d.ingredients.map(x=>x.category))].map(x=>`<option>${esc(x)}</option>`).join('')}</datalist></label><label class="field">英文名 / 别名<input name="aliases" value="${esc(i.aliases.join('，'))}" placeholder="多个别名以逗号分隔"></label><details class="material-advanced"><summary>高级匹配规则</summary><label class="check-line"><input name="matchParent" type="checkbox" ${i.matchParent!==false?'checked':''}><span>允许满足所属大类的配方要求</span></label></details><p class="hint">修改会在点击页面上方的“保存酒柜”后生效。</p><div id="item-error" class="form-error" role="alert"></div></div><div class="dialog-actions">${id?`<button type="button" class="btn danger left" data-action="delete-item">删除</button>`:''}<button type="button" class="btn" data-close="item-dialog">取消</button><button type="submit" class="btn primary">完成</button></div></form>`;
  showDialog(el);
 }
 function saveItem(form){
  try{
   const f=new FormData(form),d=itemContext==='pantry'?pantryDraft:editorDraft,old=d.ingredients.find(i=>i.id===itemId);
   const name=String(f.get('name')).trim(),kind=old?.kind||String(f.get('kind')),parentId=String(f.get('parentId')),brand=String(f.get('brand')||'').trim();
   if(!name)throw Error('请填写名称。');
   if(d.ingredients.some(i=>i.id!==itemId&&C.norm(i.name)===C.norm(name)))throw Error('同名材料已存在，请直接选择。');
   const i={id:itemId||C.uid(),name,kind,parentId,brand:kind==='product'?brand:'',category:String(f.get('category')).trim(),aliases:String(f.get('aliases')).split(/[,，]/).map(x=>x.trim()).filter(Boolean),tags:kind==='product'?f.getAll('materialTags').map(String):(old?.tags||[]),image:old?.image||'',matchParent:f.has('matchParent'),customized:true};
   if(!i.category)throw Error('请选择或填写浏览分组。');
   const check=C.clone(d);if(old)check.ingredients[check.ingredients.findIndex(x=>x.id===i.id)]=i;else check.ingredients.push(i);
   C.validate(check);d.ingredients=check.ingredients;
   if(itemContext==='pantry'&&!old&&(i.kind==='product'||!C.needsProduct(i,d)))pantryAdd(d,i);
   const callback=itemReturn;closeDialog($('#item-dialog'));
   if(callback)callback(i);else if(itemContext==='pantry')renderPantry();
   toast('已加入本次编辑，完成后请保存。');
  }catch(e){$('#item-error').textContent=e.message}
 }
 async function deleteItem(){
  const d=itemContext==='pantry'?pantryDraft:editorDraft,item=d.ingredients.find(i=>i.id===itemId);if(!item)return;
  const ids=new Set(d.ingredients.filter(i=>C.ingredientPath(i.id,d).some(p=>p.id===item.id)).map(i=>i.id));
  const formIds=$('#recipe-form')?[...$('#recipe-form').querySelectorAll('[name=ingredientId],[data-alternative]')].map(x=>x.value||x.dataset.alternative).filter(Boolean):[];
  const used=d.recipes.some(r=>[...r.ingredients,...r.garnishes,...r.versions.flatMap(v=>[...v.ingredients,...v.garnishes])].some(row=>ids.has(row.id)||(row.alternatives||[]).some(id=>ids.has(id))))||formIds.some(id=>ids.has(id));
  if(used){$('#item-error').textContent='此材料或其下级仍被配方引用，请先在相关配方中更换或移除。';return}
  const descendants=ids.size-1,owned=d.pantryItems.filter(i=>i.matches.some(id=>ids.has(id))).length;
  const detail=[descendants?`同时删除 ${descendants} 个下级类别或酒款`:'',owned?`并从酒柜移除 ${owned} 项记录`:''].filter(Boolean).join('，');
  if(await confirmChoice(`删除“${item.name}”？`,`${detail?detail+'。':''}删除会在保存当前${itemContext==='pantry'?'酒柜':'配方'}后生效。`,[['cancel','取消',''],['yes','删除','danger']])!=='yes')return;
  d.ingredients=d.ingredients.filter(i=>!ids.has(i.id));d.pantryItems=d.pantryItems.filter(i=>!i.matches.some(id=>ids.has(id))&&!i.tags.some(id=>ids.has(id)));
  const callback=itemReturn;closeDialog($('#item-dialog'));if(callback)callback();else if(itemContext==='pantry')renderPantry();toast('已从本次编辑中删除，完成后请保存。');
 }
 function confirmChoice(title,message,choices=[['yes','确认','primary'],['cancel','取消','']]){return new Promise(resolve=>{const el=$('#confirm-dialog');el.innerHTML=`<h2>${esc(title)}</h2><p>${esc(message)}</p><div class="confirm-actions">${choices.map(([id,label,cls])=>`<button class="btn ${cls}" data-choice="${id}">${esc(label)}</button>`).join('')}</div>`;const handler=e=>{const b=e.target.closest('[data-choice]');if(b){el.removeEventListener('click',handler);el.oncancel=null;el.close();resolve(b.dataset.choice)}};el.addEventListener('click',handler);el.oncancel=()=>{el.removeEventListener('click',handler);resolve('cancel')};el.showModal()})}
 function download(data){const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='mixbook-'+new Date().toISOString().slice(0,10)+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
 function resolveConflicts(result,local,remote,choice){const selected=choice==='remote'?remote:local;for(const key of result.conflicts){const [section,...rest]=key.split(':'),id=rest.join(':');if(['recipes','ingredients','pantryItems'].includes(section)){result.data[section]=result.data[section].filter(x=>x.id!==id);const value=selected[section].find(x=>x.id===id);if(value)result.data[section].push(C.clone(value))}else if(section==='favorites'){if(id in selected[section])Object.defineProperty(result.data[section],id,{value:selected[section][id],enumerable:true,writable:true,configurable:true});else delete result.data[section][id]}}const recipeIds=new Set(result.data.recipes.map(x=>x.id));for(const id of Object.keys(result.data.favorites))if(!recipeIds.has(id))delete result.data.favorites[id];return C.validate(result.data)}
 async function importData(file){if(!file)return;if(file.size>1024*1024){toast('文件超过 1 MiB，请使用不含内嵌图片的数据备份。');return}try{const other=C.validate(JSON.parse(await file.text()));const result=C.merge(C.empty(),env.data,other);let choice='local';if(result.conflicts.length){choice=await confirmChoice('导入内容存在不同版本',`${result.conflicts.length} 项记录与本机不同。未冲突的记录会合并，选择仅影响冲突项。`,[['local','冲突项保留本机',''],['remote','冲突项采用导入','primary'],['cancel','取消','']]);if(choice==='cancel')return}else if(await confirmChoice('合并导入数据',`将合并 ${other.recipes.length} 份配方和 ${other.ingredients.length} 种材料，现有记录会保留。`)!=='yes')return;const merged=resolveConflicts(result,env.data,other,choice);if(await update(d=>Object.assign(d,merged))){render();toast('数据已合并并保存。')}}catch(e){toast('导入失败：'+e.message)}}
 function scheduleSync(allowPantry=false){clearTimeout(saveTimer);if(config.auto&&token&&navigator.onLine&&(!editing()||allowPantry))saveTimer=setTimeout(()=>{if(!editing()||allowPantry)sync(false,allowPantry)},1800)}
 function tokenStorage(){try{localStorage.setItem('mixbook.config',JSON.stringify(config));localStorage.removeItem('mixbook.token');sessionStorage.removeItem('mixbook.token');if(token)(remember?localStorage:sessionStorage).setItem('mixbook.token',token)}catch{toast('此浏览器不能记住连接设置；本次仍可尝试连接。')}}
 function validateConfig(c){if(!/^[A-Za-z0-9-]{1,100}$/.test(c.owner)||!/^[A-Za-z0-9_.-]{1,100}$/.test(c.repo))throw Error('请填写有效的 GitHub 用户名与仓库名。');if(!/^[A-Za-z0-9_./-]+\.json$/.test(c.path)||c.path.startsWith('/')||c.path.split('/').some(p=>!p||p==='.'||p==='..'))throw Error('数据路径应是相对路径，以 .json 结尾，例如 mixbook.json。');if(c.branch.length>200)throw Error('分支名过长。')}
 const repoUrl=()=>`https://api.github.com/repos/${encodeURIComponent(config.owner)}/${encodeURIComponent(config.repo)}`;
 const fileUrl=()=>repoUrl()+'/contents/'+config.path.split('/').map(encodeURIComponent).join('/');
 async function api(url,options={}){const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),20000);try{return await fetch(url,{...options,signal:controller.signal,cache:'no-store',credentials:'omit',headers:{Accept:'application/vnd.github+json',Authorization:'Bearer '+token,'X-GitHub-Api-Version':'2022-11-28',...(options.body?{'Content-Type':'application/json'}:{})}})}catch(e){throw Error(e.name==='AbortError'?'连接超时，请稍后重试。':'无法连接 GitHub。修改仍保留在本机。')}finally{clearTimeout(timeout)}}
 function apiError(res){if(res.status===401)return Error('令牌无效或已过期，请重新填写。');if(res.status===403||res.status===429)return Error('权限不足或 GitHub 暂时限制请求。请检查 Contents 读写权限后稍后重试。');if(res.status===404)return Error('找不到仓库或分支，请检查名称以及令牌的仓库权限。');if(res.status===409)return Error('云端刚发生了修改，本次没有覆盖。请再次同步。');if(res.status===422)return Error('GitHub 拒绝写入，请检查分支保护、路径或仓库初始化状态。');return Error('GitHub 暂时无法处理请求（'+res.status+'）。')}
 function decodeContent(content){const bytes=Uint8Array.from(atob(content.replace(/\s/g,'')),c=>c.charCodeAt(0));return new TextDecoder().decode(bytes)}
 function encodeContent(text){const bytes=new TextEncoder().encode(text);let s='';for(let i=0;i<bytes.length;i+=8192)s+=String.fromCharCode(...bytes.subarray(i,i+8192));return btoa(s)}
 async function sync(interactive=true,allowPantry=false,direction='merge'){
   if(syncBusy)return{ok:false,message:'同步正在进行中。'};
   if(editing()&&!allowPantry){const message='请先保存或取消当前编辑，再同步。';if(interactive)toast(message);return{ok:false,message}}
   if(!token||!config.owner||!config.repo){const message='先填写私有仓库与访问令牌。';if(interactive){view='settings';render();toast(message)}return{ok:false,message}}
   if(!navigator.onLine){const message='当前离线，修改仍在本机。';if(interactive)toast(message);return{ok:false,message}}
   syncBusy=true;updateHeader();clearTimeout(saveTimer);let message='';
   try{
     validateConfig(config);await writeQueue;
     const res=await api(repoUrl());if(!res.ok)throw apiError(res);const repo=await res.json();if(!repo.private)throw Error('请连接一个私有数据仓库，避免公开酒柜与个人笔记。');
     const branch=config.branch||repo.default_branch;const br=await api(repoUrl()+'/branches/'+encodeURIComponent(branch));if(!br.ok)throw apiError(br);
     const syncId=[config.owner,config.repo,branch,config.path].join('/');
     const snapshot=C.clone(env.data),resFile=await api(fileUrl()+'?ref='+encodeURIComponent(branch));let remote=null,remoteOriginal=null,sha=null;
     if(resFile.ok){const file=await resFile.json();if(file.type!=='file'||file.encoding!=='base64'||file.size>1024*1024)throw Error('云端文件不是受支持的小型 JSON 数据文件。');sha=file.sha;remoteOriginal=JSON.parse(decodeContent(file.content));remote=C.installCatalog(remoteOriginal,window.MIX_SEED)}else if(resFile.status!==404)throw apiError(resFile);
     let target;
     if(direction==='download'&&!remote){message='仓库中还没有数据文件，无法下载。本机数据没有改动。';return{ok:false,message}}
     if(direction==='upload')target=snapshot;
     else if(direction==='download')target=remote;
     else if(!remote){if(!interactive){message='云端没有数据文件，请手动连接并确认首次写入。';return{ok:false,message}}if(await confirmChoice('首次写入 GitHub',`将把当前 ${snapshot.recipes.length} 份配方与酒柜数据保存到 ${config.repo}/${config.path}。`)!=='yes')return{ok:false,cancelled:true};target=snapshot}
     else if(!env.base||env.syncId!==syncId){if(!interactive){message='首次连接需要你选择数据，请前往同步与数据。';return{ok:false,message}}const choice=await confirmChoice('发现云端酒谱',`云端有 ${remote.recipes.length} 份配方，本机有 ${snapshot.recipes.length} 份。可采用云端数据，或合并两份记录。`,[['remote','采用云端','primary'],['merge','合并两份',''],['cancel','取消','']]);if(choice==='cancel')return{ok:false,cancelled:true};if(choice==='remote')target=remote;else{const m=C.merge(C.empty(),snapshot,remote);if(m.conflicts.length){message='首次合并存在同名编号冲突。请先导出本机备份，再采用云端并通过导入合并。';return{ok:false,message}}target=m.data}}
     else{const result=C.merge(env.base,snapshot,remote);if(result.conflicts.length){if(!interactive){message=`有 ${result.conflicts.length} 项同步冲突，请点击立即同步处理。`;return{ok:false,message}}const choice=await confirmChoice('两台设备修改了同一条记录',`检测到 ${result.conflicts.length} 项冲突。不同记录的修改会正常合并，下面的选择仅作用于冲突项。取消后可先导出备份。`,[['local','冲突项保留本机',''],['remote','冲突项采用云端','primary'],['cancel','取消','']]);if(choice==='cancel')return{ok:false,cancelled:true};target=resolveConflicts(result,snapshot,remote,choice)}else target=C.validate(result.data)}
     if(direction!=='download'&&(!remote||!C.same(target,remoteOriginal))){if(new TextEncoder().encode(JSON.stringify(target,null,2)).length>1024*1024)throw Error('初版同步文件上限为 1 MiB，请先导出备份并精简数据。');const put=await api(fileUrl(),{method:'PUT',body:JSON.stringify({message:'Update Mixbook recipes and pantry',content:encodeContent(JSON.stringify(target,null,2)),branch,...(sha?{sha}:{})})});if(!put.ok)throw apiError(put);const saved=await put.json();sha=saved.content.sha}
     await persist({...env,data:C.validate(target),base:C.clone(target),syncId,sha,lastSync:C.now()});message='已与 GitHub 同步。';return{ok:true,message};
   }catch(e){message=e.message||'同步失败，本地修改已保留。';return{ok:false,message}}finally{syncBusy=false;if(!editing()&&(view!=='settings'||!document.activeElement?.closest('#sync-form')))render();if(message){if(interactive||!message.startsWith('已与'))toast(message);const error=$('#sync-error');if(error&&!message.startsWith('已与'))error.textContent=message}updateHeader()}
 }
 function updateHeader(){const el=$('.top-status .status-text');if(el)el.textContent=status()}
 function showSyncOverlay(state='loading',message=''){const el=$('#sync-overlay'),card=el.querySelector('.sync-overlay-card'),indicator=el.querySelector('.sync-indicator'),actions=el.querySelector('.sync-overlay-actions');if(state==='hidden'){el.hidden=true;el.setAttribute('aria-hidden','true');return}el.hidden=false;el.setAttribute('aria-hidden','false');card.dataset.syncState=state;indicator.className='sync-indicator '+(state==='loading'?'sync-spinner':state==='success'?'sync-success':'sync-error');indicator.innerHTML=state==='success'?icon('check'):state==='error'?icon('close'):'';$('#sync-overlay-title').textContent=state==='loading'?'正在同步中':state==='success'?'同步成功':'同步失败';$('#sync-overlay-message').textContent=message||(state==='loading'?'正在与 GitHub 同步数据，请稍候…':state==='success'?'数据已安全保存到 GitHub。':'本地修改仍然保留，你可以再次尝试。');actions.hidden=state!=='error'}
 async function manualSync(){const direction=await confirmChoice('选择同步方向','上传会用本机完整数据覆盖仓库文件；下载会用仓库完整数据覆盖本机。建议操作前先导出备份。',[['upload','上传本机到仓库','primary'],['download','从仓库下载到本机',''],['cancel','取消','']]);if(direction==='cancel')return;showSyncOverlay('loading',direction==='upload'?'正在上传本机数据到 GitHub…':'正在从 GitHub 下载仓库数据…');const result=await sync(true,false,direction);if(result?.ok){showSyncOverlay('success',direction==='upload'?'本机数据已上传到 GitHub。':'仓库数据已下载并保存到本机。');await new Promise(resolve=>setTimeout(resolve,1100));showSyncOverlay('hidden')}else if(result?.cancelled)showSyncOverlay('hidden');else showSyncOverlay('error',result?.message)}
 async function discardDraft(){if(!editing())return true;if(await confirmChoice('放弃尚未保存的修改？','当前编辑内容会丢弃，已保存的数据不受影响。',[['cancel','继续编辑',''],['yes','放弃修改','primary']])!=='yes')return false;pantryDraft=null;editorDraft=null;for(const id of ['item-dialog','material-dialog','editor-dialog'])if($('#'+id).open)closeDialog($('#'+id));return true}
 function previewPantryType(drawer){const picker=drawer?.closest('.type-cascade');if(!picker)return;picker.querySelectorAll('[data-type-drawer]').forEach(x=>{const active=x===drawer;x.classList.toggle('active',active);x.setAttribute('aria-selected',String(active))});picker.querySelectorAll('[data-type-options]').forEach(x=>x.hidden=x.dataset.typeOptions!==drawer.dataset.typeDrawer)}
 function filterPantryTypes(input,restore=false){const dropdown=input.closest('.type-dropdown'),query=restore?'':C.norm(input.value);dropdown.open=true;const active=dropdown.querySelector('[data-type-drawer].active')?.dataset.typeDrawer;dropdown.querySelectorAll('[data-type-options]').forEach(group=>{let matches=0;group.querySelectorAll('[data-type-name]').forEach(label=>{const visible=!query||label.dataset.typeName.includes(query);label.hidden=!visible;if(visible)matches++});group.hidden=query?!matches:group.dataset.typeOptions!==active})}
 document.addEventListener('mouseover',e=>previewPantryType(e.target.closest('[data-type-drawer]')));
 document.addEventListener('focusin',e=>previewPantryType(e.target.closest('[data-type-drawer]')));
 document.addEventListener('input',e=>{if(e.target.matches('[data-type-search]'))filterPantryTypes(e.target)});
 document.addEventListener('focusin',e=>{if(e.target.matches('[data-type-search]')&&e.target.value===e.target.dataset.selectedDisplay)filterPantryTypes(e.target,true)});
 document.addEventListener('click',async e=>{if(e.target.matches('[data-type-search]')){e.preventDefault();filterPantryTypes(e.target,e.target.value===e.target.dataset.selectedDisplay)}for(const dropdown of document.querySelectorAll('.type-dropdown[open]'))if(!dropdown.contains(e.target))dropdown.removeAttribute('open');for(const wrap of document.querySelectorAll('.inline-option-add:not([hidden]),.inline-quick:not([hidden])')){if(wrap.contains(e.target)||wrap.previousElementSibling?.contains(e.target))continue;const input=wrap.querySelector('input');if(!input?.value.trim()){wrap.hidden=true;if(wrap.previousElementSibling)wrap.previousElementSibling.hidden=false}}const b=e.target.closest('button');if(!b)return;
 if(b.dataset.close){if(b.dataset.close==='editor-dialog')editorDraft=null;closeDialog(document.getElementById(b.dataset.close));scheduleSync();return}
 if(b.dataset.view){if(!await discardDraft())return;view=b.dataset.view;render();window.scrollTo({top:0,behavior:'instant'});return}
 if(b.dataset.detail){openDetail(b.dataset.detail);return}
 if('version'in b.dataset){openDetail(b.dataset.recipe,b.dataset.version);return}
 if(b.dataset.addVersion){openEditor(b.dataset.addVersion,'version');return}
 if(b.dataset.editVersion){openEditor(b.dataset.recipe,'version',b.dataset.editVersion);return}
 if(b.dataset.variant){openEditor(b.dataset.variant,'variant');return}
 if(b.dataset.edit){openEditor(b.dataset.edit);return}
 if(b.dataset.duplicate){openEditor(b.dataset.duplicate,'duplicate');return}
 if(b.dataset.editItem){openItem('pantry',b.dataset.editItem);return}
 if(b.dataset.pantryGroup){const open=!pantryOpen.has(b.dataset.pantryGroup);open?pantryOpen.add(b.dataset.pantryGroup):pantryOpen.delete(b.dataset.pantryGroup);b.setAttribute('aria-expanded',String(open));b.querySelector('.pantry-chevron')?.classList.toggle('open',open);b.nextElementSibling?.classList.toggle('open',open);return}
 if(b.dataset.spiritEntry){if(pantryDraft)pantryBrowser.navigate(b.dataset.spiritEntry);else{pantrySection=b.dataset.spiritEntry;pantryTags=[];renderPantry()}return}
 if(b.dataset.pantrySection){pantrySection=b.dataset.pantrySection;pantryTags=[];renderPantry();return}
 if(b.dataset.pantryTag){pantryTags=pantryTags.includes(b.dataset.pantryTag)?pantryTags.filter(id=>id!==b.dataset.pantryTag):[...pantryTags,b.dataset.pantryTag];renderPantry();return}
 if(b.dataset.pantryBottle){openPantryBottle(b.dataset.pantryBottle);return}
 if(b.dataset.editPantryBottle){closeDialog($('#item-dialog'));openPantryBottleForm(b.dataset.editPantryBottle);return}
 if(b.dataset.recipeMaterialRoot){const row=b.closest('.ingredient-edit-row'),material=editorDraft.ingredients.find(i=>i.id===b.dataset.recipeMaterialRoot),search=row.querySelector('[data-type-search]');row.querySelector('[name=ingredientId]').value=material.id;search.value=recipeIngredientName(material.id,editorDraft);search.dataset.selectedDisplay=search.value;b.closest('.type-dropdown').removeAttribute('open');row.querySelector('[data-action=reveal-specific]').disabled=false;return}
 if(b.dataset.typeDrawer){previewPantryType(b);return}
 if(b.hasAttribute('data-add-tag')){$('#pantry-tag-rows').insertAdjacentHTML('beforeend','<div class="tag-edit-row" data-tag-row=""><input name="tagName" maxlength="100" placeholder="标签名称"><button type="button" class="icon-button" data-remove-tag aria-label="删除标签">'+icon('trash')+'</button></div>');return}
 if(b.hasAttribute('data-remove-tag')){b.closest('[data-tag-row]').remove();return}
 if(b.hasAttribute('data-choose-material')||b.hasAttribute('data-add-alternative')){const el=b.closest('.ingredient-edit-row'),alternative=b.hasAttribute('data-add-alternative');openMaterial(i=>{const row=readEditorRow(el);if(alternative){if(i.id===row.id||row.alternatives.includes(i.id)){toast('此材料已经在这一行中。');return}row.alternatives.push(i.id)}else{row.id=i.id;row.alternatives=row.alternatives.filter(id=>id!==i.id)}el.outerHTML=editorRow(row)},el.querySelector('[name=ingredientId]').value);return}
 if(b.hasAttribute('data-remove-alternative')){b.closest('[data-alternative]').remove();return}
 if('favorite'in b.dataset){const id=b.dataset.favorite;if(await update(d=>d.favorites[id]=!d.favorites[id])){if(view==='recipes')renderResults();if($('#detail-dialog').open)openDetail(id,selectedVersion);updateHeader()}return}
 if('baseFilter'in b.dataset){filters.base=b.dataset.baseFilter;render();return}
 if(b.dataset.filter){const k=b.dataset.filter,v=b.dataset.value;filters[k]=filters[k].includes(v)?filters[k].filter(x=>x!==v):[...filters[k],v];render();return}
 if(b.dataset.availability){filters.availability=b.dataset.availability;renderResults();return}
 if(b.dataset.sort){sort=b.dataset.sort;render();return}
 if(b.dataset.removeNewTag!==undefined){const form=b.closest('form'),draft=pantryBottleDraft(form);draft.newTags.splice(Number(b.dataset.removeNewTag),1);openPantryBottleForm(itemId,String(new FormData(form).get('rootId')||''),draft);return}
 if(b.dataset.addRow){$('#editor-'+b.dataset.addRow).insertAdjacentHTML('beforeend',editorRow({id:'',amount:'',unit:b.dataset.addRow==='garnishes'?'片':'ml',optional:false}));return}
 if(b.dataset.addOption){const k=b.dataset.addOption,el=$(`[data-new-option="${k}"]`),v=el.value.trim();if(!v)return;const field=$(`[data-options="${k}"]`),inputs=[...field.querySelectorAll('.chips input')],existing=inputs.find(x=>x.value===v);if(k==='glasses'||k==='sources')inputs.forEach(x=>x.checked=false);if(existing)existing.checked=true;else{editorDraft.options[k].push(v);field.querySelector('[data-reveal-option]').insertAdjacentHTML('beforebegin',k==='glasses'?`<label class="glass-option" title="${esc(v)}"><input type="radio" name="glasses" value="${esc(v)}" checked><span class="glass-option-icon">${glassIcon(v)}</span><span class="glass-option-name">${esc(v)}</span></label>`:`<label class="chip pick-chip"><input type="${k==='sources'?'radio':'checkbox'}" name="${k}" value="${esc(v)}" checked>${esc(v)}</label>`)}el.value='';return}
 if(b.dataset.revealOption){const wrap=b.nextElementSibling;b.hidden=true;wrap.hidden=false;wrap.querySelector('input').focus();return}
 if(b.dataset.delete||b.dataset.deleteVersion){const isVersion=!!b.dataset.deleteVersion;if(await confirmChoice(isVersion?'删除此比例？':'删除这份配方？',isVersion?'基础配方和其他比例会保留。':'关联的变体会保留并成为独立配方，下次同步时会同步此次删除。',[['cancel','取消',''],['yes','删除','danger']])==='yes'){const id=editorId;if(await update(d=>{if(isVersion){const r=d.recipes.find(r=>r.id===id);r.versions=r.versions.filter(v=>v.id!==b.dataset.deleteVersion);r.updatedAt=C.now()}else{d.recipes=d.recipes.filter(r=>r.id!==id);for(const r of d.recipes)if(r.parentId===id)r.parentId='';delete d.favorites[id]}})){closeDialog($('#editor-dialog'));editorDraft=null;render();if(isVersion)openDetail(id);scheduleSync();toast('已删除。')}}return}
 switch(b.dataset.action){
 case'reveal-specific':{const wrap=b.nextElementSibling;wrap.hidden=false;b.hidden=true;wrap.querySelector('[data-specific-name]').focus();break}
 case'confirm-specific':{const row=b.closest('.ingredient-edit-row'),parentId=row.querySelector('[name=ingredientId]').value,name=row.querySelector('[data-specific-name]').value.trim(),kind=row.querySelector('.specific-kind input:checked').value,parent=editorDraft.ingredients.find(i=>i.id===parentId);if(!parent||!name)break;let made;if(kind==='name'){made=editorDraft.ingredients.find(i=>i.kind==='product'&&i.parentId===parentId&&C.norm(i.name)===C.norm(name));if(!made){made={id:C.uid(),name,kind:'product',parentId,brand:'',category:parent.category,aliases:[],tags:[],image:'',matchParent:true,customized:true};editorDraft.ingredients.push(made)}}else{const entry=pantryMenu(parentId),rootId=entry?.[0]||parentId,root=editorDraft.ingredients.find(i=>i.id===rootId)||parent;let tag=editorDraft.ingredients.find(i=>i.kind==='type'&&i.parentId===rootId&&C.norm(i.name)===C.norm(name));if(!tag){tag={id:C.uid(),name,kind:'type',parentId:rootId,brand:'',category:root.category,aliases:[],tags:[],image:'',matchParent:true,customized:true};editorDraft.ingredients.push(tag)}if(parentId===rootId)made=tag;else{made=editorDraft.ingredients.find(i=>i.kind==='type'&&i.parentId===parentId&&i.tags?.length===1&&i.tags[0]===tag.id);if(!made){made={id:C.uid(),name,kind:'type',parentId,brand:'',category:parent.category,aliases:[],tags:[tag.id],image:'',matchParent:true,customized:true};editorDraft.ingredients.push(made)}}}const value=readEditorRow(row);value.id=made.id;row.outerHTML=editorRow(value);break}
 case'new':openEditor();break;
 case'reset-filters':filters={q:'',tags:[],base:'',sourceName:'',availability:'none',favorite:false};render();break;
 case'toggle-filters':filterOpen=!filterOpen;render();break;
 case'toggle-pantry-filters':pantryFilterOpen=!pantryFilterOpen;render();break;
 case'show-ready':filters={q:'',tags:[],base:'',sourceName:'',availability:'ready',favorite:false};view='recipes';render();break;
 case'add-pantry-bottle':openPantryBottleForm();break;
 case'add-inline-pantry-tag':{const form=b.closest('form'),input=form.elements.newTagName,name=input.value.trim();if(!name)break;const draft=pantryBottleDraft(form),rootId=String(new FormData(form).get('rootId')||'');if(draft.newTags.some(x=>C.norm(x)===C.norm(name))||pantryContext(rootId).tags.some(x=>C.norm(x.name)===C.norm(name))){$('#item-error').textContent='标签名称不能重复。';break}draft.newTags.push(name);openPantryBottleForm(itemId,rootId,draft);break}
 case'reveal-inline-pantry-tag':{b.hidden=true;const wrap=b.nextElementSibling;wrap.hidden=false;wrap.querySelector('input').focus();break}
 case'reset-pantry-bottle':openPantryBottleForm(itemId);break;
 case'edit-pantry-tags':openPantryTags();break;
 case'delete-pantry-bottle':if(await confirmChoice('删除这条酒柜记录？','记录会彻底从酒柜删除；酒谱和分类目录不会改变。')==='yes'){const id=itemId;if(await update(d=>{d.pantryItems=d.pantryItems.filter(i=>i.id!==id)})){closeDialog($('#item-dialog'));renderPantry();updateHeader();scheduleSync();toast('酒柜记录已删除。')}}break;
 case'pantry-edit':if(syncBusy){toast('请等待同步完成。');break}pantryDraft=C.clone(env.data);pantryQ='';render();break;
 case'pantry-cancel':pantryDraft=null;pantryQ='';render();scheduleSync();break;
 case'pantry-save':{if(await update(d=>{d.pantryItems=C.clone(pantryDraft.pantryItems);d.ingredients=C.clone(pantryDraft.ingredients)})){pantryDraft=C.clone(env.data);pantryBrowser.data=pantryDraft;pantryBrowser.render();updateHeader();scheduleSync(true);toast('酒柜已保存。')}break}
 case'new-pantry-item':openItem('pantry');break;
 case'new-recipe-item':openItem('recipe');break;
 case'delete-item':await deleteItem();break;
 case'remove-row':{const row=b.closest('.ingredient-edit-row');if(row.parentElement.id==='editor-ingredients'&&row.parentElement.children.length===1)toast('至少需要一种原料。');else row.remove();break}
 case'export':download(env.data);toast('已生成数据备份。');break;
 case'import':$('#import-file').click();break;
 case'open-sync-settings':openSyncSettings();break;
 case'sync-now':case'retry-sync':await manualSync();break;
 case'cancel-sync-result':showSyncOverlay('hidden');break;
 case'disconnect':if(syncBusy){toast('请等待同步结束。');break}if(await confirmChoice('断开 GitHub 连接？','本机和云端数据都会保留，只移除此设备的连接设置和令牌。')==='yes'){config={owner:'',repo:'',branch:'',path:'mixbook.json',auto:false};token='';remember=false;tokenStorage();await persist({...env,base:null,sha:null,syncId:'',lastSync:null});if($('#sync-dialog').open)closeDialog($('#sync-dialog'));render()}break;
 case'clear-local-data':if(await confirmChoice('清空本地数据？','此操作无法撤销。配方、酒柜、收藏和同步设置都会从此浏览器中删除。')==='yes'){clearTimeout(saveTimer);if(db)db.close();localStorage.removeItem('mixbook.config');localStorage.removeItem('mixbook.token');sessionStorage.removeItem('mixbook.token');await new Promise((resolve,reject)=>{const request=indexedDB.deleteDatabase('mixbook');request.onsuccess=resolve;request.onerror=()=>reject(request.error);request.onblocked=()=>reject(Error('数据库正在被其他页面使用，请关闭其他页面后重试。'))});location.reload()}break;
 case'remove-samples':{const n=env.data.recipes.filter(r=>r.sample).length;if(!n){toast('没有未修改的初版示例。');break}if(await confirmChoice('移除初版示例？',`移除 ${n} 份未修改示例，IBA 酒谱会保留。`)==='yes'){await update(d=>{const ids=new Set(d.recipes.filter(r=>r.sample).map(r=>r.id));d.recipes=d.recipes.filter(r=>!ids.has(r.id));for(const r of d.recipes)if(ids.has(r.parentId))r.parentId='';for(const id of ids)delete d.favorites[id]});render()}break}
 }
 });
 document.addEventListener('input',e=>{if(e.target.id==='recipe-search'){filters.q=e.target.value;renderResults()}if(e.target.id==='pantry-search'){pantryQ=e.target.value;renderPantry()}});
 document.addEventListener('change',async e=>{const t=e.target;if(t.hasAttribute('data-recipe-material')){const row=t.closest('.ingredient-edit-row'),material=editorDraft.ingredients.find(i=>i.id===t.value),search=row.querySelector('[data-type-search]');row.querySelector('[name=ingredientId]').value=material?.id||'';search.value=material?recipeIngredientName(material.id,editorDraft):'';search.dataset.selectedDisplay=search.value;t.closest('.type-dropdown').removeAttribute('open');row.querySelector('[data-action=reveal-specific]').disabled=false;return}if(t.name==='recipeKind'){const input=t.form.elements.parentId;input.disabled=t.value!=='variant';if(!input.disabled)input.focus();return}if(t.name==='rootId'&&t.closest('#pantry-bottle-form')){openPantryBottleForm(itemId,t.value,pantryBottleDraft(t.form));return}if(t.name==='availability'){filters.availability=t.value;renderResults();return}if(t.name==='kind'&&t.closest('#item-form'))$('#item-form [data-brand-field]').hidden=t.value!=='product';if(t.name==='parentId'&&t.closest('#item-form')){const d=itemContext==='pantry'?pantryDraft:editorDraft,parent=d.ingredients.find(i=>i.id===t.value);if(parent)$('#item-form [name=category]').value=parent.category}if(t.id==='base-filter'){filters.base=t.value;renderResults()}if(t.id==='source-filter'){filters.sourceName=t.value;renderResults()}if(t.id==='favorite-filter'){filters.favorite=t.checked;renderResults()}if(t.id==='import-file'){await importData(t.files[0]);t.value=''}});
 document.addEventListener('submit',async e=>{const form=e.target;if(!['recipe-form','item-form','pantry-bottle-form','pantry-tags-form','sync-form'].includes(form.id))return;e.preventDefault();if(form.id==='recipe-form'){await saveRecipe(form);return}if(form.id==='item-form'){saveItem(form);return}if(form.id==='pantry-bottle-form'){await savePantryBottle(form);return}if(form.id==='pantry-tags-form'){await savePantryTags(form);return}if(syncBusy)return;const f=new FormData(form),next={owner:String(f.get('owner')).trim(),repo:String(f.get('repo')).trim(),branch:String(f.get('branch')).trim(),path:String(f.get('path')).trim(),auto:f.has('auto')};try{validateConfig(next);const nextToken=String(f.get('token')).trim();if(next.auto&&!nextToken)throw Error('开启自动同步前，请填写访问令牌。');config=next;token=nextToken;remember=f.has('remember');clearTimeout(saveTimer);tokenStorage();closeDialog($('#sync-dialog'));render();toast(`同步设置已保存，自动同步已${config.auto?'开启':'关闭'}。`);if(config.auto)await sync(true)}catch(err){const error=$('#sync-error');if(error)error.textContent=err.message}});
 $('#editor-dialog').addEventListener('cancel',e=>{e.preventDefault();$('#editor-dialog [data-close]').click()});
 $('#sync-dialog').addEventListener('cancel',e=>{e.preventDefault();closeDialog(e.currentTarget)});
 window.addEventListener('beforeunload',e=>{if(editing()){e.preventDefault();e.returnValue=''}});
 window.addEventListener('online',()=>{updateHeader();if(config.auto&&!editing())sync(false)});
 document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible'&&config.auto&&!editing())sync(false)});
 setInterval(()=>{if(document.visibilityState==='visible'&&config.auto&&!editing())sync(false)},60000);
 render();
 if('serviceWorker'in navigator&&/^https?:$/.test(location.protocol)&&!window.MIX_STANDALONE)navigator.serviceWorker.register('./sw.js').catch(()=>{});
 if(config.auto&&token)sync(false);
})();
